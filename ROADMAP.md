# LONG NIGHT — роадмап и контекст проекта

> Этот файл — память проекта между сессиями. Любой ИИ-агент должен прочитать его первым,
> чтобы понять состояние игры, договорённости с владельцем и что делать дальше.

## Что это
Браузерный arena-roguelike в духе Vampire Survivors. **Один файл `game.html`** (HTML+CSS+JS,
без внешних библиотек): Canvas 2D + Web Audio API. Язык интерфейса — русский.

**Концепция (важно):** мы играем за **человека-охотника на нечисть**, который использует
**заклинания и оружие на стороне Света**. Главный босс — **вампир**. Раньше игра называлась
«Багровая Орда» и была «про вампиров»; сейчас переименована и переосмыслена.

- **Название:** `LONG NIGHT`
- **Репозиторий:** `emorozoff/long-night` (слаг ссылки менять не нужно)
- **Live:** https://emorozoff.github.io/long-night/ (GitHub Pages, деплой из main, ~1 мин)
- **Исходник:** `/Users/egor/Desktop/Claude Code/вампир fable/game.html`

## Договорённости с владельцем
- **Версия игры видна в меню.** Каждое изменение, даже мелкое = бамп версии (0.12 → 0.13 → …).
  Версия задаётся константой `VERSION` в game.html и показывается в меню + угловой плашкой.
- **Каждый апгрейд — это ПРЕДМЕТ** со своей иконкой, в тематике Света (Библия, свеча, сапоги,
  наручи, крест, святая вода…). У каждого предмета будет визуальная иконка-спрайт.
- **Пиксель-арт только для игрового мира** (враги/герой/босс/фон/оружие/иконки). UI (меню,
  карточки, шрифты) остаётся чистым и читаемым (как в самом VS).
- **Музыка:** готик-оркестр, CC0/без АП. **Прогрессивная загрузка** — сначала играет 1 трек,
  остальные докачиваются со временем; если трек не докачался, продолжает играть что есть.
  Библиотека до ~20 МБ. На старт страницы загрузка музыки влиять не должна (ленивая).
- **Производительность — приоритет:** должно тянуть сотни врагов на iPhone. Только `drawImage`
  в кадре, без `shadowBlur`/`filter`, анимация через общий счётчик кадров, каппы партиклов.
- **Коммитить — ТОЛЬКО когда владелец скажет** (правило изменено 16.06.2026; раньше было «автономно»).
  Деплой на GitHub Pages — по-прежнему отдельный шаг (клон в /tmp). Локальный репозиторий проекта — в папке
  игры (`/Users/egor/Desktop/Claude Code/вампир fable/.git`), НЕ путать со случайным репо в `/Users/egor/`.

## 🎯 СОГЛАСОВАННЫЙ ПЛАН РАЗРАБОТКИ (зафиксирован 16.06.2026)

Большой арк — **переработка боссов и локаций (план C)** + хотфиксы. Порядок работ:

### 📥 ОЧЕРЕДЬ ПРАВОК ВЛАДЕЛЬЦА (по итогам игры — СВЕЖЕЕ, делать в первую очередь)
> Владелец много наиграл и выписывает правки; здесь они в порядке приоритета (группы = заходы). ИИ-агент: бери группами, проверяй headless.

> **🆕 ЗАХОД 13 — «тёплый старт» (правки от 20.06.2026). ОБСУЖДАЕМ, ЖДЁТ выбора + «приступай».** Жалоба владельца: старт очень сложный, но после прокачки норм; и не успеваешь заработать золото, чтобы начать качаться в Склепе.
> 1. **Монеты — тёплый старт:** в начале забега падают/добываются В 2 РАЗА СИЛЬНЕЕ, плавно к 6:00 → как сейчас (×1). Реализация: множитель `coinMul(m)=clamp(2 - m/6, 1, 2)` на шанс монеты (стр.1773, `cc=(0.004+e.xp*0.0035)*COINK*coinMul`). После 6 мин — без изменений.
> 2. **Сложность (HP+урон врагов) — warmup:** враги в начале СЛАБЕЕ, плавно усиливаются «со временем» до текущего baseline. Симметрично монетам (владелец: «так же ... со временем») → предлагаю достигать текущего к ~6:00. Реализация: `DIFFICULTY.warm(m)` (старт ~0.65×→1× к 6 мин) × `mul`, применять к `hp`/`dmg` в spawnEnemy. Поздняя игра (после 6 мин) НЕ меняется — там работают существующие кривые `hp(m)/dmg(m)`. Это и есть постоянная ручка «насколько лёгкий старт». ⚠️ Уточнить старт-долю (0.65×?) и горизонт (6 мин?).
> 3. **«Каждый новый уровень сложнее» — РЕШЕНО: «уровень» = ЛОКАЦИЯ** (выбор владельца 20.06.2026). Сейчас сложность завязана ТОЛЬКО на ВРЕМЯ (`m=run.time/60`), одинаково на всех локациях. ДОБАВИТЬ множитель силы врагов (HP+урон) на локацию, в порядке открытия (grave→swamp→ash→crypt→inferno): **1.00 / 1.12 / 1.25 / 1.38 / 1.50**. Реализация: поле `diff` в каждой записи `LOCS` (стр.677-682), кэш `run.locDiff` в newRun (рядом с `goldMul`), множить `hpMul`/`dmgMul` в spawnEnemy на `run.locDiff`. Множители — тунабл (можно круче). Боссов (spawnBoss) не трогаем — у них уже растёт тир по локации.

> **🆕 ЗАХОД 10 — правки от 20.06.2026 (мобайл + большой реворк прокачки оружия). ОБСУЖДАЕМ, ЖДЁТ выбора + «приступай».** Мобильность через `_isTouch()`.
> **A. Мобильная версия (через `_isTouch()`):**
> 1. **Весь UI меньше** — CSS-скейл HUD/меню/кнопок/текста на мобиле (media-query/класс).
> 2. **Зум-аут камеры** — герой/мобы/снаряды/всё мельче (видно больше поля). Реализация: глобальный `ZOOM` (1 десктоп, ~0.8 мобайл) в `render()` (`ctx.translate(W/2,H/2);ctx.scale(ZOOM);ctx.translate(-px,-py)`), И масштабировать радиусы спавна/деспавна на 1/ZOOM (иначе враги заспавнятся в видимой зоне). Самый сложный мобильный пункт.
> 3. **Монеты −30% ОТНОСИТЕЛЬНО всего** (поверх зума) — на мобиле coin-рендер ×1.5×0.7. `drawGems` стр.3872.
> 4. **Убрать качание кристаллов опыта** — `bob=Math.sin(g.t*4)*2.5` (стр.3864), для XP не качать.
> **B. Меню:**
> 5. **Заголовок в главном меню ушёл вбок на мобиле — выровнять.** Причина вероятно: `letter-spacing:4px` (трейлинг после посл. буквы) + `min(104px,15vw)` ширина на узком экране (стр.77, `.titlewrap` 76). Фикс: компенсировать трейлинг-letter-spacing (text-indent/margin) + центрирование + проверить на 375px.
> 6. **Убрать «Сохранение/Загрузка» из Склепа** (`scrShop` стр.353-361: `btnSaveToggle`+`expWrap`+экспорт/импорт). ⚠️ ВОПРОС: удалить совсем или ПЕРЕНЕСТИ в Настройки (сохранить бэкап-функцию)?
> **C. Стрелки (ranged: spitter/archer/cultist; НЕ боссы):**
> 7. **Вдвое меньше стрелков** — урезать счётчики в `doWave` (archers/spitters/cultists) + веса/элиту вдвое.
> 8. **Снаряды −30% скорости** — `vx/vy` ranged-снаряда 240→168 (стр.2316).
> 9. **Скорострельность −30%** — `e.atkT` 2.4→~3.1 (стр.2314).
> 10. **Снаряд +30% размер** — `r:5`→~6-7 (стр.2316) + визуал снаряда.
> **D. 🔧 БОЛЬШОЙ РЕВОРК — прокачка оружия за монеты в Склепе (владелец просит продумать логику):**
> - Суть: в Склепе вкладываешь монеты в оружие → растёт сразу НЕСКОЛЬКО параметров (перманентно, во всех забегах). Текущая сила оружия = ПОТОЛОК (полная прокачка); базу ОСЛАБИТЬ → без вложений играть СЛОЖНЕЕ, монетами восстанавливаешь/усиливаешь.
> - ПРЕДЛОЖЕННАЯ ЛОГИКА (детали в чате 20.06, ЖДЁТ выбора): `meta.wcrypt[id]` = тир оружия 0..5, покупается в Склепе (цена растёт). Хук `wMul(id)`=множитель урона от тира, `wCd(id)`=множитель скорострельности; база ослаблена ~×0.7, полный тир ≈ текущая сила. Внутриранговый левел-ап (lv1-5) ОСТАЁТСЯ (предположение — подтвердить). ВАРИАНТЫ пакета на тир: (A) равномерный урон+скорострельность [v1, просто]; (B) per-weapon фирменные статы [богаче, дольше]; (C) гибрид. ⚠️ ВОПРОСЫ владельцу: пакет A/B/C? · база ослабляется на сколько (потолок = текущее или выше)? · левел-ап в забеге остаётся?

> **🆕 ЗАХОД 9 — правки от 19.06.2026 (ОБСУЖДАЕМ, ЖДЁТ выбора по вопросам + «приступай»):**
> 1. **Ускорить ВСЮ игру ×1.4 (+40%) — «медленная».** РЕШЕНИЕ: глобальный множитель времени — `update(dt×1.4)` в главном цикле (стр.4858, рядом с `*devTimescale`; добавить `const GAMESPEED=1.4`). Всё масштабируется ВМЕСТЕ (игрок/враги/снаряды/спавны/кулдауны/XP/таймеры) → относительный баланс СОХРАНЯЕТСЯ, геймплей не ломается. Кламп `Math.min(dt,0.05)` уже есть (макс шаг ×1.4=0.07 — безопасно). ⚠️ ВОПРОС владельцу: длина ночи. (а) оставить 15:00 игрового времени → ночь кончается на ~40% быстрее по реальным часам (~10:45), кривая сложности per игровая-минута та же [ПРОСТО, рекоменд.]; (б) сохранить ~15 реальных минут → поднять временные пороги боссов/директора ×1.4. Реал-тайм анимации (мерцание факелов через performance.now) не ускорятся — косметика. Делать ПЕРВЫМ (влияет на ощущение сложности → п.2 тюнить ПОСЛЕ).
> 2. **Усложнить игру — ВАРИАНТЫ владельцу.** Сейчас: `hpMul=1+0.55m+0.17m²`, `dmgMul=1+0.22m`, скорость до +40%, cap 330 (стр.1759-1764, diffNow 3199). Рычаги: A — круче рост HP/урона врагов; B — гуще спавн (cap↑/интервал↓/batch↑); C — боссы сильнее/чаще; D — меньше сустейна игроку (XP медленнее/лечение меньше); E — **СЕЛЕКТОР СЛОЖНОСТИ** (Норма/Сложно/Кошмар = множители статов врагов, самый гибкий — не ломает для тех, кто хочет легче); F — элита чаще. ⚠️ Учесть: ускорение ×1.4 (п.1) САМО усложняет (меньше времени на реакцию) — тюнить сложность ПОСЛЕ п.1. Жду выбор рычагов.
> 3. **Синергия на карточке оружия (выбор при левел-апе) — пиксельная подпись «с чем соединяется».** На карточке оружия (`renderChoices` стр.1522) добавить чип: партнёр по СЛИЯНИЮ (`FUSIONS` — оружие+оружие) + партнёр по ЭВОЛЮЦИИ (`EVOS` — оружие+пассивка). Пиксельный стиль (иконка партнёра + «⮂»/«★» + название результата). ⚠️ Нужно добавить поле `partner` в `EVOS` (id пассивки) для отображения — сейчас он спрятан в `req`-лямбде. Слияния берутся из `FUSIONS.pair`.
> 4. **30 FPS в Chrome на GitHub-ссылке (стабильно, как кап).** ОТВЕТ: в коде НЕТ кап-а FPS — цикл `loop` (стр.4850) на `requestAnimationFrame` (целится в refresh монитора, обычно 60). Стабильные 30 = ВНЕШНЕЕ: (а) Chrome аппаратное ускорение ВЫКЛ → canvas на CPU (chrome://gpu → «Canvas: Hardware accelerated»? Настройки→Система→«Использовать аппаратное ускорение»); (б) энергосбережение/батарея троттлит rAF до 30 (подключить питание/выкл battery-saver); (в) монитор реально 30 Гц (напр. 4K@30 по HDMI — проверить настройки экрана ОС); (г) игра не тянет 60 на слабом GPU (но это не «кап», а просадка → плановая оптимизация: пулинг/адаптивные каппы). Действие: владелец проверяет среду (chrome://gpu + питание + Гц экрана). Можем и профилировать/оптимизировать рендер отдельным заходом.
> 5. **Курсор: прятать через 1с покоя, показывать при движении.** Сейчас в игре курсор СКРЫТ всегда (`body.ingame *{cursor:none!important}` стр.295). Сделать: видим обычно; mousemove-хендлер с таймером 1с → класс `body.idle{cursor:none}`; при движении снять `idle` + перезапустить таймер. На тач-устройствах курсора и так нет. Мелкая JS+CSS правка.
>
> **✅ РЕШЕНИЯ владельца (19.06.2026) + ВЫПОЛНЕНО (0.75):** FPS (п.4) — проблема была на стороне владельца, СНЯТО. Скорость — постоянная **×1.5** (`GAMESPEED`, как таймскейл дев-панели; ускоряет ВСЁ). Сложность — **+20%** через ЕДИНЫЙ регулятор `DIFFICULTY{mul,hp,dmg,spd,bossHp,bossDmg}` (легко крутить дальше, в т.ч. «легко в начале / ад на 10-й мин» = переформировать кривые), **БЕЗ изменения густоты спавна** (по просьбе). Синергия-чипы (п.3) и курсор (п.5) — сделаны. ⚠️ На будущее: ускорение ×1.5 + ×1.2 + рамп плотности (через run.time) дают суммарно жёстче кривую по реальному времени — тюнить по плейтесту через `DIFFICULTY`.

> **🎵 БУДУЩЕЕ — МАКСИМАЛЬНОЕ СЖАТИЕ МУЗЫКИ (записано 18.06.2026, отдельным заходом).** Владелец хочет проверить, насколько сильно можно ужать треки, чтобы 8-бит-стиль ВСЁ РАВНО звучал нормально. Гипотеза: даже ~50 КБ может быть ок. ЗАДАЧА: сделать НЕСКОЛЬКО вариантов сжатия каждого трека в диапазоне **~50 КБ … ~500 КБ** (напр. ступени 50/100/200/350/500 КБ — играть битрейтом/моно/частотой дискретизации через ffmpeg, исходники сохранить). Дать владельцу ПРОСЛУШАТЬ и сравнить (временное мини-меню сравнения ИЛИ просто положить файлы-варианты, чтобы он выбрал ступень). Он выберет, на какой ступени звук ещё приемлем → ту и оставить (перезалить track1..5, поднять sw CACHE). ⚠️ «Прослушать музыку» из настроек убрано (0.71) — для теста вариантов понадобится временный плеер ИЛИ A/B вне игры. Сейчас музыка ~4 МБ (48k); цель — радикально меньше.

> **🆕 ЗАХОД 8 — правки от 18.06.2026. ✅ ЗАХОД 8A ВЫПОЛНЕН (0.71): монеты-C (п.5), фонарь (п.3), могильщик (п.4), лужи-озёра (п.8) + UI U1–U4. Проверено headless, НЕ закоммичено. ОСТАЛОСЬ: 8B (боссы: п.6 Палач, п.9 Утопленница, Пастырь A+C, Король B+C+D−дэш) + 8C (достижения U5/п.2).**
> 1. **Монеты притягиваются издалека и «медленно ползут».** Корень: монета магнитится тем же радиусом, что и опыт (`magR=magBase*magMul`, `magBase:38`, до ~100px с прокачкой), а после подбора-триггера `g.mag=true` остаётся НАВСЕГДА → редкая монета тянется за бегущим игроком через пол-экрана с ПОСТОЯННОЙ скоростью 380px/с (без разгона; коммент в коде прямо: «далёкий летит долго»). С опытом не видно (его много), а одинокая монета визуально «ползёт издалека». `updateGems` стр.3232–3236. ВАРИАНТЫ — см. вопрос владельцу (меньше радиус для монет / быстрее притяжка с разгоном / и то и то).
> 2. **🥇 Достижения: убрать доход + всплывающие карточки В БОЮ.** (а) Убрать золото за ачивки — «игроки открывают их для себя». Сейчас `checkAchievements` (стр.1799) делает `meta.gold+=a.gold`, в «Летописи» (`buildStats` стр.4780) показывается «◆50». Убрать начисление + ценник золота из строки достижения. (б) Сейчас ачивки проверяются ТОЛЬКО в конце забега (`recordRun`→`checkAchievements`) и показываются общим баннером. Надо: проверять В РЕАЛЬНОМ ВРЕМЕНИ во время боя и при разблокировке показывать **вплывающую карточку** с названием/описанием достижения, в стиле игры (пиксельная рамка, золотая печать, плавный вплыв-увод). Стиль продумать самому. ⚠️ Карточка = игровой оверлей (серая зона UI), могу сделать функциональную стилизованную версию; финальную полировку при желании отдадим UI-агенту.
> 3. **Фонарь — поворот 1500°/с + гаснет, если рядом никого.** Базовый конус: `maxTurn` сейчас `TAU*3` (1080°/с) → сделать **1500°/с** (`TAU*(1500/360)`). Плюс «гаснет, если рядом никого нет» = когда в радиусе нет цели, НЕ рисовать конус-свечение (сейчас `render` стр.3509 рисует конус всегда). Синергия-луч (spear) — БЕЗ изменений (остаётся `TAU`=360°/с и своя отрисовка). `fireWeapons` стр.2015, `render` стр.3509.
> 4. **Могильщик — зомби ×1.5, без опыта.** Призыв «Поднятие мёртвых» сейчас `n=[5,7,9]` за волну по фазам (`BOSS_MECH.gravedigger` стр.2673) → ×1.5 ≈ **[8,11,14]**. ⚠️ Опыт с поднятых УЖЕ = 0 (`gg.xp=0`, стр.2679) — эта часть просьбы уже выполнена; проверю, что они и монет не роняют (вопрос владельцу).
> 5. **РЕШЕНО монеты = вариант C** (18.06): малый радиус притяжки именно для монет (~50px вместо общего magR) + резкое УСКОРЕНИЕ (стартует медленно → разгон → «дзынь» в руку). Опыт/сердца — без изменений. `updateGems` стр.3225–3236: ввести отдельную ветку для `kind==='coin'` (свой триггер-радиус + накопление скорости `g.spd`).

> **🆕 ЗАХОД 8 (продолжение) — правки от 18.06.2026 (по скрину Палача + аудит боссов + лужи уровней + реворк Утопленницы):**
> 6. **Палач (flayer) — лужи как ЖИДКОСТЬ + больше + бросок слизи в игрока.** (а) Сейчас `flayPools` рисуются как ровные круги (`drawZones` стр.3459) — переделать форму на НЕРОВНУЮ, «жидкую» (полигон с шумом по краю / метаболы, пиксельная заливка). (б) Увеличить размер (`poolR=[58,70,84]` → крупнее). (в) НОВАЯ атака: Палач периодически **кидает слизь В игрока** — снаряд летит в текущую позицию игрока и при попадании/падении создаёт лужу ПРЯМО под игроком (`spawnFlayPool` в точке игрока) → заставляет постоянно убегать. Лужа наносит урон (уже есть DoT) + **отравление ~2с** (нужен короткий debuff игрока: тик-урон 2с — сейчас `run.flaySlowT` только slow; добавить `run.poisonT`). `BOSS_MECH.flayer` стр.2389, `updateFlayPools` стр.2374.
> 7. **АУДИТ механик боссов — ответ владельцу (детали в чате 18.06).** Итог: 6 из 8 боссов имеют ОТЛИЧНЫЕ нестандартные механики (Матерь=рой; Людоед=пожирает мобов и растёт; Палач=лужи area-denial; Могильщик=призыв+ловушки-могилы; Дева=фантомы-отражения+телепорт; Король=неуязвим пока не зажжёшь 6 факелов). Самый «снаряды+беги» = **Тёмный пастырь (shepherd)** — 3 паттерна вороньих снарядов (линия/дуга/нова) + пике, спец-взаимодействия нет. **Король (deadking)** механику ИМЕЕТ (факелы), но если игрок не замечает баннер — читается как «неуязвимый кидает снаряды». ВОЗМОЖНЫЕ доработки (предложить владельцу): пастырю — уникальную механику (напр. зона-«туман»/вороны выклёвывают/щит из стаи); королю — яснее телеграф факелов (стрелки/пульс). ЖДЁТ решения владельца.
> 8. **Лужи на уровнях — ×4 крупнее + сильнее замедляют игрока (per-локация, ВЫБОР владельца).** Сейчас лужи `poolAt` r=60–115px; замедление ИГРОКА: grave/swamp→0.84, crypt→НЕ замедляет (по лору «ледяная вода замедляет нечисть, не тебя»). Локации с лужами: **grave** (Затонувшая деревня), **swamp** (Кровавая топь), **crypt** (Затопл. склеп). Огненные (ash/inferno) — разломы, не лужи. ВАРИАНТЫ замедления/размера — см. таблицу в чате. ⚠️ crypt: если замедлять игрока — ломаем фишку «не тебя» (флаг владельцу). `poolAt` стр.1236 (размер), player-slow стр.3122, enemy-slow стр.2163.
> 9. **Утопленница (drowned) — НОВАЯ механика (реворк).** Заменить текущие расходящиеся кольца-приливы на: (а) вокруг самой Утопленницы — постоянные ЛУЖИ, наносящие урон игроку (area-denial у её тела). (б) ПРИТЯЖЕНИЕ игрока ЛАВИНАМИ воды: лавина появляется ЗА краем экрана, идёт В СТОРОНУ Утопленницы (не от неё), проходит СКВОЗЬ игрока и сильно подталкивает его К Утопленнице (а не от). Толчок направлен к боссу. (в) Вид — вода в пиксель-стиле с ЗЕЛЁНЫМ оттенком (не нынешний круг). Переписать `updateTides`/`BOSS_MECH.drowned` стр.2452–2527: волна = направленная полоса (фронт-линия) с вектором к боссу, толчок игрока вдоль вектора волны (к боссу). ⚠️ Визуал воды — серая зона (сделаю функционально, лоск可 UI-агенту).

> **✅ РЕШЕНИЯ владельца (18.06.2026):**
> - **Лужи уровней — делать «как рекомендую»:** РАЗМЕР per-локация (grave = Размер-B, ×4 радиуса 240–460px «озёра»; swamp = Размер-A, ×2 радиуса 120–230px; crypt = Размер-A). ЗАМЕДЛЕНИЕ игрока = Вариант 1: **grave ×0.62, swamp ×0.72, crypt 0 (НЕ трогать — фишка «лёд тормозит нечисть, не тебя»)**. Враги в лужах — как сейчас (crypt сильнее). ⚠️ Реализация: `poolAt` (стр.1236) — масштабировать `r` по `run.loc`; для grave РАЗДВИНУТЬ сетку пулов (ячейка 300→~600), иначе озёра сольются в сплошную воду. player-slow `*0.62/0.72` по loc (стр.3122).
> - **🆕 ПРАВИЛО ПРОЕКТА: ВСЕ мобы, которых создаёт ЛЮБОЙ босс, дают 0 опыта.** Зафиксировано 18.06.2026. Сейчас уже соблюдено (matron gnat `xp=0`, drowned minion `xp=0`, shepherd bat-эскорт `xp=0`, gravedigger risen `xp=0`, maiden phantom `xp=0`, людоед — пожирает без killEnemy). ПРИ ЛЮБОЙ новой боссовой способности со спавном — ставить `gg.xp=0`. (Монеты с них — по умолчанию НЕ роняем тоже, раз это «фантомы боя», подтвердить если всплывёт.)
> - **Доработка способностей ПАСТЫРЯ + Короля** — владелец просит ПРЕДЛОЖИТЬ крутые/неожиданные (уточнил 18.06: пастырь, НЕ палач — палач и так апается лужами/слизью). ✅ ВЫБРАНО 18.06:
>   - **ПАСТЫРЬ (shepherd) = A + C.** **A «Стампид»:** раз в N сек зов → притягивает ВСЕХ обычных мобов на экране к себе, бафает (ускорение) и гонит лавиной-тараном на игрока (оружие из имеющейся орды, не плодит новых). **C «Вороний туман»:** сажает на землю зоны кружащего воронья — внутри DoT (выклёвывание) + затемнение обзора, area-denial. Существующие вороньи снаряды-паттерны (линия/дуга/нова) — оставить, но УРЕЗАТЬ частоту, чтобы A+C стали фирменными, а не фон. `BOSS_MECH.shepherd` стр.2528.
> **🆕 ЗАХОД 8 (UI-блок) — правки от 18.06.2026. ⚠️ ВЛАДЕЛЕЦ РАЗРЕШИЛ МНЕ ДЕЛАТЬ UI («Ты отныне сам можешь делать UI») — см. память [[longnight-agent-zones]]. Координировать с UI-агентом только арт-спрайты (ENEMY_MAPS/HEROSPR/BOSS_DRAW).**
> - **U1. Убрать «ПРОСЛУШАТЬ МУЗЫКУ» из настроек.** Удалить секцию `<div class="setsec">ПРОСЛУШАТЬ МУЗЫКУ</div>` + `<div id="musTest">` (стр.364–365), функцию `buildMusicTest` (стр.4675), слушатель `musTest` (стр.4880), вызовы в `openSettings` (buildMusicTest/onStatus/onPrevEnd + лишний `MUSIC.loadAll`). Сам движок MUSIC не трогать.
> - **U2. Слайдеры громкости — пиксельный стиль, цифры читаемы.** Сейчас `input[type=range]` нативный (`accent-color:var(--red)`, стр.253). Сделать квадратные пиксельные трек+ползунок (`::-webkit-slider-runnable-track/thumb`, `::-moz-range-track/thumb`), проценты `.setval` оставить читаемыми (золотые, чёткий шрифт).
> - **U3. Иконка ▸ на кнопке «НА ОХОТУ» — крупнее.** `<span class="ar">▸</span>` (стр.299), CSS `.btn.big .ar` (стр.98) — увеличить размер/жирность стрелки.
> - **U4. Бестиарий — пиксельное оформление, текст читаем.** `buildBestiary` (стр.4762) + `.bcard` CSS (стр.156–162): пиксельная рамка карточки, заголовки шрифтом Tiny5 (`var(--pix)` — кириллица ок), описания оставить читаемыми. Спрайты врагов не трогаю (зона UI-агента).
> - **U5. 🏆 ДОСТИЖЕНИЯ — большой реворк (объединить с Заходом 8 п.2 «карточки в бою + убрать золото»):** (а) ГОРАЗДО больше ачивок (сейчас 8 → ~24–30, категории: убийства/время/боссы/комбо/оружие-макс/эволюции/слияния/локации/герои/монеты/уровни/без урона и т.п.). (б) Каждая ачивка = **прогресс cur/goal** (не bool) → ПОЛОСКА ЗАПОЛНЕНИЯ (напр. «Палач: 5000/10000» = 50%). Менять структуру `ACHIEVEMENTS` (стр.1785): вместо `check:m=>bool` → `cur:m=>number, goal:number` (+ иконка, + единица). (в) Красивые ПИКСЕЛЬНЫЕ карточки с ИКОНКОЙ у каждой, текст Tiny5 читаем. (г) СОРТИРОВКА по % заполнения (по убыванию — почти готовые/готовые вверху). (д) Золото за ачивки УБРАНО (п.2). (е) Live-проверка в бою + всплывающая карточка при разблокировке (п.2). `buildStats` ачивки (стр.4774–4782), CSS `.ach` (стр.163–166), `checkAchievements` (стр.1796) → live + карточка. Полный список ачивок — драфт в чате, согласовать с владельцем.
>   - **КОРОЛЬ (deadking) = B + C + D, и УБРАТЬ дэш.** **B «Тьма»:** периодически тушит мир — виден лишь круг вокруг игрока + горящие факелы (рендер виньетки/света; в тему LONG NIGHT). **C «Костяной феникс»:** на низком HP (напр. ≤20%) рассыпается в кости, снова `invuln=true`, факелы гаснут → СРОЧНО пере-зажги, пока собирается обратно (вторая фаза-загадка под занавес). **D «Указ короля»:** бьёт мечом в землю → по полу расходятся ЛИНИИ костяных шипов узором (крест/кольца), телеграф → урон, обходить между забегами к факелам. **➖ УБРАТЬ «типичное ускорение с красной подсветкой»** = удалить у короля generic-дэш (телеграф `e.chT`→рывок `e.dashT`, красная линия) — он у всех боссов, королю не нужен; давление держим его уникальными способностями (тьма/шипы/феникс/веер снарядов) + обычная погоня шагом. `BOSS_MECH.deadking` стр.2842 (движение 2858–2869). ⚠️ Визуал короля/анимация — зона UI-агента; делаю механику + функц. рендер тьмы/шипов.


> **🗺️ КАРТА ЗАХОДОВ (согласовано с владельцем 16.06.2026 — брать побольше за раз):**
> - **Заход 1 «Стабилизация» ✅ ВЫПОЛНЕНО в 0.37** = ПРИОРИТЕТ №0 (логи + честный фикс инфляции опыта) **+ Группа B** (дроп с босса + баланс) **+ Группа A** (весь UI-полиш) **+ фикс шрифта (Tiny5, кириллица) глобально**. Проверено headless: 78 полевого XP → ровно +3 ур. (честная математика), босс роняет 0 XP. ОСТАЛОСЬ из Группы A: иконки/кнопки меню по «скрину 2» (нужен скрин от владельца) + переименование Nox Atra/пасхалка (не срочно).
> - **Заход 2 «Айдентика и полиш» ✅ ВЫПОЛНЕНО (0.38–0.39)** = **Группа F** (меню на арте + анимация, 0.38) **+ Группа E** (звук фонаря, зона тапа версии, aura → «Кадило», 0.39). Ребрендинг Nox Atra **ОТМЕНЁН** — владелец оставил «LONG NIGHT» (16.06.2026). Музыка — в самом конце roadmap.
> - **Заход 3 «Оружие и синергии»** = **Группа C** (бафы Креста/Святой воды, новое оружие Меч, проблема aura — защита от снарядов разными путями) **+ Группа D** (слияние оружия / переработка синергий). По каждому — варианты владельцу ПЕРЕД кодом.
> - **Заход 4 «Орда гуще + экономика» ✅ ВЫПОЛНЕНО (0.47)** = +14 врагов до **21 типа** (гуль/скелет/утопленник/личинка/латник/гончая/бомба/лучник/культист/паук/огр/призрак/гарпия/жук) с поведениями (chase/swarm/hop/ranged/tank/warp + новые **fast**/**exploder**, паук **split**); **конечные тематические волны** (трикл = только фоддер, опасные типы — конечными группами в `doWave`, не вечно); **экономика** — монеты по силе врага (`cc=0.004+xp*0.0035`, value ~xp). Бестиарий обновлён. ОСТАЛОСЬ из Захода 4: **боссы #2–8** (механики+спрайты) — следующими заходами по одному.
> - Логика порядка: сначала «не сломано» (Заход 1 ✅) → «красиво/узнаваемо» (2) → «приятно играть» (3) → «больше контента» (4). Порядок гибкий, владелец может менять.

> **✅ ЗАХОД 5 «Экономика + буст босса + МУЗЫКА» ВЫПОЛНЕН (0.49, 17.06.2026):** A — монеты ×20 реже (`cc/20`), цены Склепа ÷10 (`metaCost /10`), монета ×1.5 визуально, новый SFX `A.coin()` (звон). B — Матерь роя: скорость ×1.4, мошки ×3 (per-burst [12,18,27], потолок 70→210, фаза-2 выброс 18→54). C — 5 треков владельца сжаты ffmpeg 96k (18МБ→8.8МБ) в `music/track1..5.mp3`, MUSIC переписан (роли menu=t1/calm=t3/intense=t2/boss=t4/tension+final=t5), **бесшовный кроссфейд-луп** (2 элемента/дека, `applyDeck`), **меню прослушивания** в настройках (статус загрузки ●, ▶/■), атрибуция убрана, sw CACHE→0.49. Проверено headless: цены 25→3/300→30, монета/дроп/звон в живом коде, мошка 12/выброс, все 5 треков 200 + статус «ok», роли босс→t4/уровень→t3, консоль чистая. **НЕ закоммичено — ждёт команды владельца.**
>
> **🗒️ ЗАХОД 5 (исходная запись правок):**
>
> **A. Экономика / монеты (уточнено 17.06.2026):**
> - **Монеты выпадают в 20 раз РЕЖЕ** — режем ШАНС дропа `cc` ÷20 (`killEnemy`, Заход 4: `cc=0.004+e.xp*0.0035`). Номинал не трогаем — монета редкая, но ценная, «звон» = событие.
> - **Цены в Склепе в 10 раз МЕНЬШЕ** — все стоимости `META_DEFS` ÷10 (мета-прогрессия).
> - **Монетка крупнее ×1.5 (визуально)** — спрайт/масштаб отрисовки пикапа-монеты `kind:'coin'` (НЕ менять хитбокс/баланс, только рендер). Где: `drawGems` / спрайт `SPR.coin`.
> - **Звук «звон» при подборе монетки** — новый SFX в объекте `A` (короткий металлический «дзынь»), играть в момент подбора coin-пикапа (там же, где начисляется золото в `updateGems`).
>
> **B. Матерь роя — СИЛЬНЕЕ (пилот-босс):**
> - **Мошки ×3** — втрое больше спавна adds (мошкары) от босса.
> - **Скорость босса +40%** — увеличить `spd` Матери роя на 40%.
>
> **C. Музыка — владелец принёс 5 СВОИХ треков (детали и mood — в разделе «🎵 МУЗЫКА» внизу файла; делать в рамках этого захода, НЕ откладывать в конец).** Кратко: треки в `music/` («1 трек.mp3»…«5 трек.mp3»), сделать лёгкими (ffmpeg), встроить, **меню прослушивания/проверки загрузки**, **бесшовный плавный луп** (конец→начало того же трека через кроссфейд), **убрать подпись названия трека в настройках**.

> **🆕 ЗАХОД 6 «Баланс оружия» — СОГЛАСОВАНО с владельцем 17.06.2026, ЖДЁТ «приступай» (пока UI-агент делает визуал боссов):**
> - **Метательный крест (scythe) — кол-во ÷2.** Урон/скорость/всё прочее — БЕЗ изменений, режем только число крестов за залп. Сейчас `n=(lv>=3?3:2)+(evo?2:0)`, роза(lv5)=8/12. Станет ~вдвое меньше (норм. 1/2, роза 4/6, эво +1). `fireWeapons` ~стр.1972.
> - **Святая вода (mortar) — слабее.** (а) на **lv1 — ровно ОДНА лужа** (сейчас `n=(2+lv)` → lv1=3; станет `n=lv` → 1/2/3…). (б) **частота −20%** (интервал ×1.25): `wt.mortar=Math.max(0.9,2.4-0.18*lv)…` → база медленнее. `fireWeapons` ~стр.1988/1996.
> - **Фонарь (lantern) — РАЗДЕЛИТЬ скорость поворота.** Синергия «луч» (spear) — тонкий длинный луч, поворот **360°/с** — УЖЕ верно, не трогать. БАЗОВЫЙ фонарь (конус) я ошибочно тоже сделал 360°/с — а он был мгновенный (0.43). Вернуть «как был» = конус, но с лимитом поворота **1080°/с**. Фикс: `maxTurn=(spear?TAU:TAU*3)*dt` (`fireWeapons` ~стр.2019). ✅ ПОДТВЕРЖДЕНО владельцем: базовый Фонарь = конус + поворот 1080°/с; синергия-луч = 360°/с.
> - **Меч веры (sword) — НЕРФ (выбрать вариант с владельцем).** Проблема: в плотной куче меч бьёт «почти моментально всем» (перескакивает с цели на цель каждый кадр, только per-enemy иммунитет `swT=0.45`) и **раскидывает всех** (нокбэк 130). ✅ ВЫБРАН владельцем ВАРИАНТ 1: глобальный кулдаун удара меча (~0.25с между ударами, отдельно на каждый меч) + срезать отбрасывание (нокбэк 130→~40). Бьёт по обеим жалобам (моментальный мульти-хит + раскидывание кучи). `fireWeapons` ~стр.2059.

> **🆕 ЗАХОД 6 (продолжение) — правки от 17.06.2026:**
> - **Музыка при паузе/настройках из ПАУЗЫ — игровая, не меню.** Меню-трек (t1) играет ТОЛЬКО на главном экране (где кнопка «НА ОХОТУ»). Сейчас `MUSIC.roleNow()` отдаёт игровую музыку при playing/paused/levelup, но при `state===settings` (открыто из паузы) откатывается на меню. Фикс: считать «идёт забег» по `settingsFrom===paused` / активному `run` → держать игровую роль; меню-трек только при `state===menu`.
> - **Экономика монет — ПЕРЕСМОТР модели.** Монеты только с пола (подбор). С моба при смерти «бросок кубика»: у каждого моба коэффициент выпадения (по силе), `if(chance(coef)) → выпадает **РОВНО 1 монета (value 1)** (сейчас value=`irand(1,2+xp*1.2)` — много за раз; меняем на 1). **Итог: количество монет с мобов ×50 меньше.** Предложение: коэффициент = `(0.004+e.xp*0.0035)/50` (заменить текущий `/20`) = ~×50 от ИСХОДНОГО (Заход 4) числа дропов. ✅ ЦЕЛЬ владельца (17.06): сейчас ~500 монет за ~2 мин → хочет ~10 (×50 меньше). Калибрую делитель ЭМПИРИЧЕСКИ в браузере (симуляция реалистичного забега, счёт монет → ~10 за 2 мин). Монеты зачисляются ТОЛЬКО при физическом подборе с пола (уже так; проверить, что нет авто-зачисления помимо подбора/магнита). Влияет на Склеп (цены ÷10) — следить за играбельностью. `killEnemy` дроп монет.
> - **Сброс прогресса СОХРАНЯЕТ настройки.** Кнопка «Сброс прогресса» ×5 сейчас откатывает весь `meta` в `defaultMeta` (вместе с настройками графики/звука). Надо: сбросить прогресс (золото/анлоки/статы/бестиарий/достижения), но `meta.settings` (громкости/тряска/HP-бар/цифры/перф/FPS) ОСТАВИТЬ как были. Фикс: `const s={...meta.settings}; meta=sanitizeMeta(defaultMeta()); meta.settings=s; saveMeta();` (в обработчике `btnReset`).

> **🆕 БОЛЬШИЕ ЗАХОДЫ — добавлено 17.06.2026 (порядок предложен; ЖДЁТ «приступай»):**
> - **Музыка — сжать ЕЩЁ ×2** (8.8 МБ → ~4–4.5 МБ). ffmpeg: ~48–64 kbit/s (при необходимости моно/пониже частота). Сохранить исходники. Перезалить track1..5.mp3, поднять sw CACHE. Идёт вместе с другими муз-правками.
> - **ДЕПЛОЙ актуальной версии на GitHub Pages.** Сейчас задеплоен СТАРЫЙ билд (владелец играет в него — отсюда «500 монет»). Выкатить текущий `main` (0.57+: все боссы, UI-детализация, экономика, музыка). По CLAUDE.md §3: клон в уникальный /tmp, `game.html→index.html`, ROADMAP/manifest/sw.js/icons/music, commit, `git push origin HEAD:main`. sw CACHE поднять. CDN-лаг бинарников 404→200 (норм). ⚠️ Делать ПОСЛЕ коммита геймплейных правок, чтобы деплой был свежим. Публикация — спрошу подтверждение перед push.
> - **🆕 ЗАХОД «Мобильная оптимизация» (большой).** (1) **QA-аудит** ВСЕХ экранов/элементов на мобильных вьюпортах (меню/герои/локации/склеп/летопись/бестиарий/настройки/пауза/левел-ап/смерть/HUD) — проверить, что всё ОТКРЫВАЕТСЯ и не ломается (preview_resize 375×812 и др.). Это read-only, делаю я. (2) **CSS/раскладка фиксы** — зона UI-агента (координировать: после его визуала боссов или отдать список). (3) **Перфоманс** (моя зона): пуллинг пуль/кристаллов/партиклов, спавн ближе без деспавна, тумблер «упрощённая анимация», адаптивные каппы по FPS — из §6 плана. Разнести: аудит+перф (я) ∥ CSS (UI-агент).

**🔴 ПРИОРИТЕТ №0 — БАГ ЛАВИНЫ ОПЫТА (ломает игру, владелец писал 3 раза, прошлые «фиксы» НЕ помогли — нужен ГАРАНТИРОВАННЫЙ).**
> Симптом: после 1-го босса игрок получает **+~20 уровней** (а должен +~2); куча красных кристаллов (~15 шт);
> иногда «выдаётся ~20 предметов подряд, а полоска уровня будто поднялась чуть». Новый агент: НЕ повторяй прошлый подход
> (там лишь подняли каппы в `gemDrop` — недостаточно). Ниже — разобранный корень (числа посчитаны по коду на 0.36) и план в глубину.
>
> **РАЗБОР КОРНЯ (3 независимых источника складываются):**
> 1. **Босс роняет ОГРОМНУЮ кучу XP напрямую.** В `killEnemy` (game.html):
>    - `~1500` `gemDrop(e.x,e.y,e.xp)` — у 1-го босса `e.xp≈150` (формула `spawnBoss ~2129`: `Math.round(110*tier*(1+m*0.12))`, tier=1, ~3 мин).
>    - `~1514` цикл: **10 кристаллов по `v:20` = +200 XP**, и они **минуют `gemDrop`** (никаких каппов!).
>    - Итого с босса напрямую ≈ **350 XP = ~13 кристаллов** (совпадает с «~15 красных»). + магнит (`~1518`) + бомба + сердце.
> 2. **Поле копит до 4000 XP.** `gemDrop ~1641`: `CAP=100, MAXGEMS=40` → на поле живёт до **40×100 = 4000 XP**, если игрок кайтит босса 3 мин и не собирает.
> 3. **Магнит зачищает ВСЁ поле разом.** Пикап магнита (`~2389`) ставит `mag=true` всем xp-кристаллам → `addXP` за серию кадров
>    прокручивает `while(run.xp>=run.xpNext){ run.level++; run.lvlQueue++; }` (`~1665`). `lvlQueue` копится до ~20 → 20 экранов выбора подряд.
>    Кривая `xpNeeded(l)=floor(6+4l+l^1.85)` (`~1163`): суммарно до 20-го ур. ≈ 2700 XP, до 24-го ≈ 4200 → **4350 XP даёт +~20 уровней**. ✔ сходится.
>    («20 предметов, +2 уровня» — вероятно тот же `lvlQueue`, либо ветка `noChoices ~1306` при максах раздаёт HP/золото вместо карт; лог покажет точно.)
> ⚠️ Существующая защита `updateGems ~2357` (`if(state!=='playing') break;`) ограничивает лишь ЧИСЛО триггеров за кадр — `lvlQueue` всё равно копится до 20. Это и есть причина, почему «вроде чинили, а баг живёт».
>
> **❗ФИЛОСОФИЯ ФИКСА (требование владельца, 16.06.2026):** НИКАКИХ КОСТЫЛЕЙ. Запрещено «обрезать» число уровней за раз,
> «не давать опыт после левел-апа», жечь излишек XP и т.п. Если игрок ЧЕСТНО заработал «миллион» опыта — он ДОЛЖЕН получить
> заслуженные уровни. Задача — **найти ОТКУДА берётся «миллион» опыта (честный источник инфляции/дублирования) и устранить именно его.**
> Опыт после фикса должен быть ровно тем, что игрок реально набил — не больше и не меньше.
>
> **ГАРАНТИРОВАННЫЙ ПЛАН — найти и убрать ЧЕСТНУЮ инфляцию (никаких каппов уровней):**
> - **Шаг 1 — СИСТЕМА ЛОГОВ (идея владельца, теперь это ГЛАВНЫЙ инструмент, делаем ПЕРВЫМ).** Кольцевой буфер `window.__xplog` (~200 событий).
>   Логировать с атрибуцией источника: `gemDrop`(вход `val`, тип врага, `fieldXp` стало), `addXP`(вход XP, множители, `level` до/после, `lvlQueue`),
>   пикап xp-кристалла(`g.v`, применённый множитель, итог), пикап магнита(сколько XP всего собрано). Дамп: кнопка в дев-панели «скопировать XP-лог»
>   + `window.__xplog` в консоли. **Так источник «миллиона» становится виден по кадрам** — владелец воспроизводит, шлёт лог, видно точное число и откуда.
> - **Шаг 2 — проверить КОНКРЕТНЫЕ подозреваемые на инфляцию (это НЕ костыли — это исправление неверного подсчёта опыта):**
>   - 🔎 **(a) ДВОЙНОЙ множитель времени на боссе.** `spawnBoss ~2129` уже масштабирует `e.xp` на `(1+m*0.12)`, а при ПОДБОРЕ (`updateGems ~2371`)
>     XP ещё раз умножается на `(1+run.time/60*0.12)` → опыт босса масштабируется временем ДВАЖДЫ. Время должно применяться РОВНО ОДИН раз.
>   - 🔎 **(b) ДВОЙНОЙ дроп с босса.** `killEnemy ~1500` `gemDrop(e.x,e.y,e.xp)` срабатывает и для босса, И сверху хардкод-кольцо 10×20=200 (`~1514`).
>     Т.е. босс отдаёт опыт дважды (свой `e.xp` + кольцо). Решить, какой опыт у босса ИСТИННЫЙ, и отдавать его ОДИН раз. (По Группе B босс вообще 0 прямого XP.)
>   - 🔎 **(c) ИНФЛЯЦИЯ ОПЫТА ПРИ ПОЗДНЕМ СБОРЕ — главный честный подозреваемый.** Множитель времени берётся в момент ПОДБОРА (`~2371`), а не дропа.
>     Кристалл, выпавший на 1:00 (`v=5`), но собранный магнитом на 5:00, получает множитель 5-й минуты (×1.6) вместо ×1.12. Кайт + магнит = весь старый
>     опыт пересчитывается по поздней ставке = «миллион из ниоткуда». **Честный фикс: фиксировать ценность кристалла В МОМЕНТ ДРОПА** (запекать множитель в `g.v` в `gemDrop`/при спавне), при подборе множитель НЕ применять. Тогда кристалл стоит ровно столько, сколько заработано тогда — без инфляции от позднего сбора.
>   - 🔎 **(d) Двойной подбор / дублирование.** Убедиться, что кристалл не зачисляется дважды (помечается `dead` сразу), и сплит-слизни/спиттеры не плодят XP.
> - **Шаг 3 — устранить найденное** (по логу + (a)–(d)). После этого опыт = ровно набитое игроком. Магнит может собирать всё поле разом — это ОК,
>   потому что значения честные (никаких каппов на уровни). Сколько реально набил — столько уровней и получит.
> - **Проверка:** headless — заспавнить 1-го босса (`__game.boss(0)`), убить, сверить `run.level` ДО/ПОСЛЕ с суммой по `__xplog`. Критерий: число выданных
>   уровней = математически точная сумма реально собранного XP по кривой `xpNeeded` (а не раздутая множителями). Прогнать «кайт-кейс» (накопить кристаллы → собрать магнитом) и сверить, что поздний сбор НЕ инфлирует старый опыт.

**Группа A — мелкий UI/полиш (один быстрый заход):**
- **Убрать подсказки управления** (слишком очевидно): элемент `#hintText` (HTML ~строка 266 «WASD — движение · ESC — пауза»;
  JS ~3364 и ~3366 ставят «веди пальцем по экрану — герой идёт за ним»). Убрать/опустошить — и десктопную, и тач-подсказку.
- **Уменьшить карточки выбора предметов на МОБИЛЕ:** в `@media (max-width:640px)` (game.html ~строки 176–180) ужать `.card`
  (ширину/паддинги/`min-height`) и шрифты названия/описания (`.card .cname`, `.card .cdesc`), чтобы 3 карточки помещались на телефоне.
- **Полоса HP — плоский цвет без градиента, чуть светлее:** `#hpFill` (CSS ~строка 43) сейчас
  `background:linear-gradient(180deg,#e03048,#911426)` → сделать плоским `background:#ec4a5e` (чуть светлее верхней части #e03048).
- **Мобильный HUD занимает слишком много места (скрин владельца):**
  - **Уменьшить иконки предметов** в HUD на мобиле (слоты оружия/пассивок) — `refreshItems` + CSS HUD-чипов (`@media max-width:640px`). Сейчас сетка иконок съедает пол-экрана.
  - **Таймер наезжает на счётчик монет** — поправить раскладку верхней панели (таймер `#timer` / золото — развести, чтобы не перекрывались на узком экране).
  - Подумать, как ещё сэкономить место сверху на мобиле (компактнее ряды, меньше отступы).
- **Сокращать большие числа в пост-игровой сводке (скрин владельца):** в рендере сводки (`game.html ~3321–3344`: «Урона нанесено», «Средний DPS»,
  «Урона получено», блок «УРОН ПО ИСТОЧНИКАМ») числа идут слитно и нечитаемы (напр. `20326230`). Завести хелпер `fmtNum(n)` →
  `1.2млн` / `1.2кк` / `12.3к` и прогнать через него крупные значения. Мелкие (<1000) оставить как есть.
- **🔤 ШРИФТ НЕ ПИКСЕЛЬНЫЙ — РЕШИТЬ РАЗ И НАВСЕГДА (владелец писал многократно). ТОЧНАЯ ПРИЧИНА НАЙДЕНА (диагностика 16.06.2026):**
  - **Причина 1:** инлайненный шрифт **VT323 НЕ содержит кириллицы вообще** (`grep -c "cyrl" game.html` = 0; в шрифте только латинские
    языковые системы AZE/CAT/CRT/KAZ/MOL/ROM/TAT/TRK). → ВЕСЬ русский текст с `var(--pix)` откатывается на системный `Segoe UI`.
    Доказательство: латинское «LONG NIGHT» (`.title`) — пиксельное, кириллица рядом — нет.
  - **Причина 2:** половина UI вообще не использует `var(--pix)` — захардкожено `font-family:Georgia,serif` (CSS-строки:
    `#timeText`~47, `#bannerBig`~61, `.locchip .ln`~110, `.bcard .bq/.bname`~153/154, `.card .cname`~203, `.lvlbtn`~207,
    `.card.banish::after`~215, `.mname`~226, `#deathGrid .sec`~244, `.deathtitle`~246) + `body`~23 (`Segoe UI`).
  - **ФИКС (на всех браузерах, включая `file://`):**
    1. Заменить VT323 на пиксельный шрифт **с полной кириллицей** — **Pixel Operator** (CC0, классический пиксель, полная кириллица) ИЛИ
       **Pixelify Sans** (Google Fonts, кириллица). Инлайнить как base64 в `@font-face` (Chrome блокирует `url()`-шрифты на `file://`).
       ⚠️ Перед инлайном ПРОВЕРИТЬ покрытие кириллицы у выбранного файла (а не повторить ошибку с VT323).
    2. Применить `var(--pix)` **глобально**: задать на `body`, убрать ВСЕ перечисленные `Georgia,serif`/`Segoe`-переопределения
       (или переопределить их на `var(--pix)`). Фолбэк в самой `--pix` сделать `monospace`, не Georgia.
    3. `font-display:block`. Проверка headless И на `file://` И на `http://` — отрисовать тестовую кириллическую строку, убедиться что пиксельная.
- **🖼️ Иконки и кнопки меню — переделать по эскизу владельца (скрин 2, прислан 16.06.2026):**
  - Заменить иконки пунктов меню на более качественные пиксельные (по скрину 2: фигурка=ГЕРОИ, крест-аптечка=СКЛЕП, кубок=ДОСТИЖЕНИЯ/ЛЕТОПИСЬ,
    книга=БЕСТИАРИЙ, X=НАСТРОЙКИ). Сейчас иконки в `.btn` меню — на эмодзи/спрайтах; заменить на пиксельные спрайты в стиле скрина 2.
  - **Стиль кнопок** как на скрине 2 (тёмная заливка + красная рамка, пиксельные) — `.btn`~90 уже близок; довести до вида со скрина 2.
  - ⚠️ Новому агенту: попросить у владельца **скрин 2** (референс кнопок/иконок) — он у владельца, в репо не лежит. Связано с разделом «➕ Пиксельный UI» ниже.

**Группа B — баланс и дроп (числовые правки + переработка дропа, один заход):**
> Все правки от владельца по итогам реальной игры. Код-указатели даны для нового агента; строки плывут — ищи по символам.
- **Цена «Изгнания»:** сейчас `toggleBanish`/`banishCard` (game.html ~1355–1368) изгоняют предмет из пула и **оставляют выбор** на этом уровне.
  Сделать: после изгнания игрок **лишается выбора на этом уровне целиком** (закрыть левел-ап, как Skip). Т.е. изгнание = вместо выбора, а не вдобавок.
  Скорее всего: в `banishCard(i)` после пометки `run.banished` вызвать закрытие выбора (как `doSkip`), а не `renderChoices()`. Это и есть «цена изгнания».
- **Урон Фонаря ×0.5:** в `fireWeapons` (game.html ~1834) формула `dmg=(4+2.2*lv)*p.dmgMul*(evo?1.3:1)` → ополовинить базу (напр. `(2+1.1*lv)`).
- **Сердца (HP-дроп) ×3 реже:** в `killEnemy` (game.html ~1502) шанс `chance(0.007+run.heartDrop)` → поделить базовый шанс на 3 (`0.0023+run.heartDrop`).
  Учесть синергию `alch`/«Целебный эликсир» (game.html ~1233, `r.heartDrop+=0.012`) — её прибавку тоже уменьшить пропорционально, чтобы «чаще» осталось осмысленным.
- **Нерф каскадных взрывов (синергия «Кара света» / `shatter`):** game.html ~1478–1486. Сейчас замедленный враг при смерти бьёт соседей
  уроном `sd=(10+e.maxhp*0.15)*run.p.dmgMul` в радиусе 95 → соседи (тоже замедленные) умирают и взрываются дальше = цепная реакция, выкашивает экран.
  Это и есть «крайне имбовый взрыв», который владелец просит понерфить. Варианты (выбрать/скомбинировать при разработке):
  убрать/срезать скейл от HP (`*0.15`→`*0.05` или фикс. урон), уменьшить радиус (95→~60), добавить, что взрыв **не триггерит вторичные взрывы**
  (цепочка максимум в 1 звено — помечать `e._shattered`, чтобы убитые взрывом не взрывались сами).
- **Переработка дропа с БОССА (game.html ~1510–1522, блок `if(e.boss)`):**
  - **Босс НЕ роняет опыт вообще** — убрать цикл из 10 xp-кристаллов (`for(let i=0;i<10;...) kind:'xp'`).
  - **Босс роняет МАГНИТ**, который собирает ВЕСЬ опыт с карты (накопленный за бой с обычных мобов). Магнит уже это делает — см. `pickup magnet`
    game.html ~2388: `for(const o of run.gems) if(o.kind==='xp'||'coin') o.mag=true`. Оставить магнит в дропе босса, это и есть «награда за босса».
  - **Магнит-пикап теперь падает ТОЛЬКО с босса.** Сейчас элита тоже роняет магнит (50%) — game.html ~1508
    `kind:chance(0.5)?'magnet':'bomb'` → у элиты оставить только `'bomb'` (или сундук), магнит убрать.
- **(связано, уже в Группе… см. §item)** дроп монет по силе врага — экономика, см. блок «4. Экономика» ниже (не дублировать).

**Группа C — оружие: бафы + новое (контент/дизайн). ✅ НАПРАВЛЕНИЯ ВЫБРАНЫ владельцем (17.06.2026):**
> - **Крест:** A (чаще+шире: кулдаун 1.8→~1.0с, крестов 2/3/4, шире веер) + D (на lv5 «роза клинков» — кресты по кругу во все стороны).
> - **Святая вода:** A (гуще/дольше/чаще урон) + B (поджиг-DoT: враг в луже загорается и горит вне лужи, огонь перекидывается по толпе).
> - **Меч:** как в заметке ниже, авто-сик; уточнение владельца — **меч сам выбирает цель** (агент решает как: ближайший / приоритет по HP / цикл). Имя/эволюция — выбрать при разработке (предложено «Меч веры» + «Рой клинков»).
> - Синергии — **пересобираются заново** (см. отдельное обсуждение по каждой; «слияние оружия» Model 1 как механика).
- **Усилить «Метательный крест» (`scythe`, бумеранг)** — сейчас брать не хочется. Агент предлагает владельцу варианты усиления
  (напр.: больше снарядов/шире дуга, урон на возврате ×2, пронзает и тянет врагов, ускорение на максималках). Код: `fireWeapons` ветка scythe + `wt.scythe`.
- **Усилить «Святую воду» (`mortar`, лужа огня на полу)** — тоже непривлекательна. Варианты владельцу
  (напр.: больше/шире лужи, дольше горит, тикает чаще, поджигает врагов = DoT, лужи сливаются). Код: ветка mortar + зоны `updateZones`/`mortar`.
- **НОВОЕ ОРУЖИЕ «Меч» (авто-сик в радиусе, без перезарядки):**
  - Летает сам от врага к врагу на автопилоте, **БЕЗ перезарядки** — непрерывно.
  - Есть **радиус действия** вокруг героя — далеко от героя не улетает (привязан к игроку).
  - Если рядом НЕТ врагов — **кружит вокруг героя по большому радиусу** (холостой режим), ждёт цель.
  - Реализация: похоже на гибрид `orbit`(клинки) + самонаведение `bolt`. Отдельная сущность-снаряд с состоянием (seek/idle-orbit),
    target = ближайший враг в радиусе; долетел/ударил → выбирает следующего; нет целей → плавно уходит на орбиту вокруг `p`.
  - Чеклист нового оружия как обычно: запись в `run.weapons`/`wt`, ветка в `fireWeapons`, спрайт (`SPR.*`), иконка (`SPR.icons`),
    `WSTAT`, апгрейд-дефиниция в `UPGRADES` (kind `w`, `max:5`), при желании эволюция в `EVOS`, отрисовка в `render`, HUD-слот.
- **ПРОБЛЕМА БАЛАНСА: «Освящённая земля» (`aura`) обязательна к взятию.** Это единственное оружие, что **рассеивает вражеские снаряды**
  (`fireWeapons`, ветка aura + синергия `shatter` рассеивает вдвое быстрее) → игрок всегда её берёт, выбора по сути нет.
  Нужно дать ДРУГИЕ способы защищаться от снарядов, чтобы aura не была безальтернативной. Идеи на проработку с владельцем
  (это дизайн, обсудить перед кодом): пассивка-щит/блок снарядов (периодический оберег), редкий дроп-баф, герой с врождённой
  защитой, «парирование» при рывке/на низком HP, общий механик «снаряды дохнут о любую зону на земле» (mortar/aura/bell), и т.п.
  Цель — чтобы защита от снарядов была доступна РАЗНЫМИ путями, а aura стала одним из вариантов, а не обязаловкой.

> **✅ ЗАХОД 3 ЗАВЕРШЁН (0.41):** ✅ шаг 1 Крест (роза клинков) · ✅ шаг 2 Святая вода (кольцо огня + поджиг-DoT) · ✅ шаг 3 защита от снарядов (кольцо/клинки/кресты/колокол по касанию + пассив «Оберег») · ✅ шаг 4 «Меч веры» (авто-сик + орбита + эво «Рой клинков») · ✅ шаг 5 СИНЕРГИИ: убраны 7 старых, добавлен массив `FUSIONS` (5 фьюзов, оба оружия на lv5 → карта-слияние, фиолетовая связь в HUD `.ichip.fused`, `isFused()`); «Стальная буря» = слияние в один большой меч (Model 2), остальные — усиление. Эффекты: storm(цепь чаще/сильнее в дыму), pyre(кресты поджигают), spear(фонарь — мощный пронзающий луч), knell(колокол — больше волна + замедление).
> ⚠️ ТЕХДОЛГ: мёртвый код от старых синергий (проверки `run.taken.shatter/frost/whirl/bleed/bastion/conduct/wind` в combat) теперь всегда falsy — безвреден, можно вычистить при случае. Также блок статуса `e.bleedT` в `updateEnemies` больше никем не ставится.

**Группа D — переработка синергий / слияние оружия. ✅ ФИНАЛ ДИЗАЙН СОГЛАСОВАН (17.06.2026):**
> Принцип: «мало, но круто». Условие синергии — **ОБА оружия на lv5** → выпадает золотая карта-слияние → берёшь → активна.
> 7 старых синергий (`frost/shatter/conduct/whirl/bleed/wind/bastion`) **удаляются**, заменяются на 5 новых фьюзов. HUD: пара оружий
> подсвечивается **фиолетовым** (отдельных иконок-синергий нет). Оружие может быть в 2 фьюзах (Кадило — хаб).
> **5 СИНЕРГИЙ:**
> 1. ⚡ **Громовой алтарь** (Цепь+Кадило) — молния непрерывно бьёт по всем в дыму, перескакивая = гроза вокруг героя. [усиление обоих]
> 2. 🌀 **Стальная буря** (Клинки+Меч) — **СЛИЯНИЕ В ОДНО**: оба оружия превращаются в ОДИН огромный меч, медленно кружит вокруг игрока, сносит почти всех с удара (огромный урон). [Model 2 — заменяет оба]
> 3. 🔥 **Крестный огонь** (Крест+Вода) — кресты загораются и оставляют горящий след, враги на пути вспыхивают. [усиление обоих]
> 4. 🔆 **Копьё света** (Арбалет+Фонарь) — болты+луч сливаются в один пронзающий луч света сквозь ряд врагов. [усиление обоих]
> 5. 🔔 **Святой набат** (Колокол+Кадило) — удар колокола в дыму = огромная волна святого света: сносит снаряды, бьёт по площади, замедляет. [усиление обоих]
> Имена не конфликтуют с EVOS (Благовест/Распятие — там). Механика слияния поддерживает 2 режима: «усилить оба + фиолетовая связь» и «заменить оба на новое» (Стальная буря).
>
> **РЕВОРК БАЗОВЫХ ОРУЖИЙ (Группа C, согласовано):**
> - **Святая вода** — горшки падают **кольцом вокруг героя на фикс. радиусе** (равномерно по кругу = кольцо огня), гуще/дольше/шире/чаще (A) + **поджиг-DoT**: враг в луже загорается и горит вне лужи, огонь перекидывается по толпе (B). Защитное огненное кольцо.
> - **Крест** — A (кулдаун ~1.0с, крестов 2/3/4, шире веер) + D (на lv5 «роза клинков» — кресты по кругу во все стороны).
> - **Меч** (новое) — авто-сик, сам выбирает цель (ближайший в радиусе, ретаргет при убийстве/попадании), холостой → орбита вокруг героя. Имя «Меч веры», эволюция «Рой клинков».
> - **Защита от снарядов (чтобы Кадило не обязательна)** — добавить ПАССИВ «Оберег»: раз в ~6с гасит 1 входящий снаряд (доступно всем билдам). Базовое Кадило по-прежнему рассеивает снаряды.

**Группа D — (исходные заметки по механике слияния):**
> Повод (скрин владельца): в HUD на мобиле последние ~4 иконки синергий выглядят ОДИНАКОВО (`kind:'syn'` в `UPGRADES ~1234–1255`
> используют общий/дублирующийся спрайт) и зря занимают место. Владелец предлагает вообще убрать отдельные иконки-синергии и сменить механику.
- **Идея владельца (наугад, обсудить и допилить):** когда ДВА орудия достигают макс. уровня — их можно **«скрестить»**. Появляется
  новый предмет (может выпасть как карта). Если игрок его берёт — между этими орудиями возникает **синергия**, и оба орудия
  помечаются **фиолетовым** на панели (вместо отдельной иконки-синергии). Так HUD освобождается, а синергии становятся наглядными.
- **❓ Открытый вопрос (продумать механику):** что делать, если ПОТОМ выпадет ещё одна синергия, связывающая одно из УЖЕ
  связанных орудий с НОВЫМ орудием? (цепочки/сети синергий, конфликт пометок, можно ли орудию быть в 2 синергиях, цвет/значок для мульти-синергии).
  Агент предлагает владельцу конкретную модель ПЕРЕД реализацией.
- Связано: текущие синергии — массив `UPGRADES` (`frost/shatter/conduct/whirl/bleed/wind/bastion`), отрисовка в `refreshItems`.
  При переработке учесть существующие 7 синергий (мигрировать на новую механику или оставить часть как есть — решить с владельцем).

**Группа F — редизайн меню (по макету владельца) [0.38, 16.06.2026]:**
- ✅ **Фон-картинка владельца + анимация поверх** (процедурную силуэт-сцену заменили — владельцу не зашла). База: `art/menubg.jpg`
  (пиксель-арт сцена, ужата из `Фон меню.png` 2.3МБ → JPEG 1500px ~356КБ). Грузится в `drawMenuAmbient` (`menuBgImg`), рисуется `cover`-вписыванием.
  Слои анимации поверх (canvas, без shadowBlur): 🔴 красные глаза (пульс+моргание, 5 точек в тёмных местах), 🍂 падающие листья (~30, гуще слева у дерева),
  🌙 перелив свечения луны, ☁️ дрейф облаков, 🌫️ туман в низине, 🪟 мерцание окон замка + виньетка/скрим под UI для читаемости.
  ⚠️ **ДЕПЛОЙ:** копировать папку `art/` в репо Pages; `sw.js` ASSETS уже содержит `./art/menubg.jpg`, CACHE→0.38. Оригинал `Фон меню.png` в репо НЕ нужен (тяжёлый).
  Позиции глаз/окон/луны заданы в долях картинки (`mapX/mapY` в `drawMenuAmbient`) — если владелец сменит арт, поправить доли.
- ✅ **Рестайл кнопок** под макет: тёмный «каменный» стиль (вместо красных), `.btn`/`.btn.big` (game.html CSS ~91), красная стрелка `▸` (`.ar`).
- ✅ **Убрана кнопка «ГЕРОИ»** из меню (выбор героя остаётся в потоке НА ОХОТУ). HTML `#scrMenu` + снят wiring `btnHeroes`/`icoHeroes`.
- ⏳ Возможные доводки: яркость/позиции глаз, плотность/заметность листьев, удержание луны на вертикальной мобилке (сейчас `cover` обрезает бока — луна/дерево уходят).

**Группа E — полиш (звук / ввод / визуал оружия) ✅ ВЫПОЛНЕНО (0.39):** убран звук удара фонаря (`if(src!=='lantern') A.hitE()`); зона тапа версии увеличена (`#verCorner` padding); aura → «Кадило» (имя везде + пиксельная иконка-кадило + тонкая быстрая пульсация + тёплое свечение в `drawAura`). Ниже — исходные заметки:**
- **Убрать звук удара Фонаря.** Источник: `damageEnemy` (`game.html:1474`) зовёт `A.hitE()` на КАЖДОЕ попадание; фонарь тикает каждые
  0.22с (`fireWeapons`, ветка lantern ~1859 `wt.lantern=0.22`) → ритмичный «удар-удар» звучит неправдоподобно (фонарь не бьёт ритмично, он ЖЖЁТ светом).
  Фикс: не проигрывать `A.hitE()` для `src==='lantern'` (`if(src!=='lantern') A.hitE();`). Опц.: добавить тихий непрерывный «шип/гул горения», пока луч активен.
  ⚠️ Освящённая земля (`aura`) — тоже непрерывный источник и тоже спамит `A.hitE()`; владелец про неё не просил, но логично заодно (на усмотрение агента/владельца).
- **Увеличить зону нажатия на версию (открытие дев-панели).** `#verCorner` (CSS `game.html:126`) сейчас крошечный (`font-size:11px`), тяжело попасть.
  Фикс: увеличить кликабельную область — большой `padding` (напр. `padding:16px 22px;` + компенсирующий отрицательный `margin`, чтобы визуально не сместился),
  при желании увеличить сам шрифт чуть. Цель — крупная невидимая зона тапа в углу. (JS уже есть: `devTapVersion` ~3166, навешен на `click` ~3486.)
- **Переработать «Освящённую землю» (`aura`) → «КАДИЛО» (выбор владельца 16.06.2026). МЕХАНИКА ТА ЖЕ, меняем айдентику и визуал:**
  - **Новое название «Кадило»** (курильница на цепях; священный дым/жар вокруг героя). Логика дыма объясняет все 5 пунктов механики:
    несёт с собой (поле за героем), жжёт/душит нечисть (урон+замедление), дым-завеса гасит снаряды, благовоние освящает/лечит (эволюция).
    Места для замены текста/иконки: `WSTAT.aura` (`game.html:1141` → 'Кадило'), апгрейд-деф (`1222–1223` — описание под дым/ладан),
    описание героя Паладина (`1178` — упоминает «Освящённая земля» → «Кадило»), эволюция «Святилище» (`1282` — можно оставить или переименовать под дым, напр. «Фимиам»/«Каждение»).
  - **Иконка:** новый пиксельный спрайт `SPR.icons.aura` — **кадило на цепочках** (узнаваемое, ни на что не похоже в наборе).
  - **Пульсация: меньше амплитуда, но быстрее.** `drawAura` (`game.html:2684`): сейчас `pulse=0.85+sin(now*0.004)*0.15` (амплитуда ±0.15, медленно)
    → сделать ~`0.96+sin(now*0.012)*0.04` (амплитуда ±0.04, ~3× быстрее) — едва заметное частое «дыхание» вместо широкого пульса.
  - **Лёгкое пиксельное свечение.** Добавить мягкое свечение зоны — ⚠️ БЕЗ `shadowBlur`/`filter` (правило перфоманса): через доп. полупрозрачные
    пиксельные кольца/слои в `drawAura` или предрендеренный спрайт-глоу (`drawImage`). Лёгкое, тёплое (в тон #ffe1.. ).

### ▶ ПРОШЛЫЙ ЗАХОД ✅ ВЫПОЛНЕНО (0.29-0.36): Tiers 1–3 «Фундамент + пилот»
Один большой заход, в конце — играбельный билд, **СТОП на тест пилота**. По решению владельца
**дев-панель вынесена вперёд** (в фундамент — ускоряет тесты всех боссов).
- **Tier 1 — Хотфикс:** баг лавины опыта (репро + защита в глубину + проверка) · Свеча→Магнит +25% + иконка · убрать экранный счётчик серий.
- **Tier 2 — Фундамент:** рефактор поведения в `behavior` + фикс белых версий · **система препятствий** (тормозят только игрока) · **каркас боссов + структура план C** · **панель разработчика** (секретный жест по `#verCorner`).
- **Tier 3 — Первый срез:** **Матерь роя** целиком (спрайт + «Выводок») · переделка **локации №1 (Затонувшая деревня)** · враг **рой-мошкара**. → играбельный забег.
- **⏸ Стоп-точка:** владелец играет пилот → калибруем темп боссов #2–8.
- Коммит между тирами (автономно). Тест каждого тира headless `--mute-audio`, FPS при 250+.

### 0. Политика коммитов (всегда)
Коммитить **только по команде владельца** (правило изменено 16.06.2026; раньше — автономно).

### 1. 0.29 — Хотфикс + мелочи (СНАЧАЛА)
- 🐛 **Баг лавины опыта** ⚠️ **НЕ ИСПРАВЛЕН — прошлый фикс (0.29) оказался недостаточным.** Полный разбор корня
  (с числами) и ГАРАНТИРОВАННЫЙ план в глубину + система логов — см. выше: **«🔴 ПРИОРИТЕТ №0 — БАГ ЛАВИНЫ ОПЫТА»**
  в разделе «📥 ОЧЕРЕДЬ ПРАВОК ВЛАДЕЛЬЦА». Делать по тому плану, эту строку не использовать как руководство.
- 🧲 «Свеча» → «Магнит», эффект **+25%** радиуса сбора (было +40%), новая иконка.
- ✂️ Убрать **экранный** счётчик «×N СЕРИЯ» (`#comboTag`). Достижение «Кровавая жатва» и стату
  «Макс. серия» — оставить фоном. Аудио-`combo` (высота звука подбора) не трогать.

### 2. Боссы и локации (план C) — большой арк
Структура: **8 боссов в фиксированном порядке** появления (~каждые 3 мин, ручка баланса 2.5–3); **локация
решает, на каком боссе ночь кончается**. Общая тройка (#1–3) в каждом забеге; финалы #4–8 = 5 локаций.
Прогрессия: открыть следующую локацию = убить финального босса текущей. Абсолютный финал — Мёртвый король.

**Новые механики арки:**
- **Препятствия:** редкие коллайдеры-кружки, детерминированно по миру (как декор, но «твёрдые»), тематичны
  локации. Тормозят **только игрока**; снаряды/оружие/враги — сквозь. Лёгкая помеха манёвру.
- **Тронный зал** при выходе Мёртвого короля **запечатывается** в зал-арену; до этого — обычное поле.

**Локации (5, переосмыслены под боссов):**
| # | Локация | Палитра | Препятствия | Финал |
|---|---|---|---|---|
| 1 | Затонувшая деревня | холодная болотно-зелёная с синью | сваи мостков, крыши, коряги | Утопленница |
| 2 | Выжженные пастбища | выцветшая охра, серость | кривые столбы ограды, черепа на кольях | Тёмный пастырь |
| 3 | Погост | бурая земля, мокрый камень, туман | кресты/плиты, кучи земли, лопаты | Могильщик |
| 4 | Заброшенная усадьба | холодный бело-голубой, серебро | мебель под простынями, колонны, зеркала | Бледная дева |
| 5 | Тронный зал | поблёкшее золото/багрянец, обугленный камень | разбитые колонны, трон, доспехи | Мёртвый король |

**Боссы (8, механики зафиксированы):**
1. **Матерь роя** (общая) — кайтит прочь; плодит рой-мошкару (**0 опыта**); только адды; фазы → гуще выводок.
2. **Людоед** (общая) — прёт на игрока; **пожирает монстров на пути → лечится/крепнет** (стак HP/урон/размер); ближний бой.
3. **Гнилой палач** (общая) — удары оставляют **ядовитые/гнилостные лужи**, area-denial.
4. **Утопленница** (лок-1) — **Прилив**: волны-телеграфы катятся по арене, dodge; после волны вода замедляет.
5. **Тёмный пастырь** (лок-2) — **Зов воронья**: стаи ворон волнами/линиями (живые снаряды) + наскоки.
6. **Могильщик** (лок-3) — **Поднятие мёртвых** (площадной спавн дохликов у игрока, **0 опыта**) + **разрытые могилы**-ловушки под игроком.
7. **Бледная дева** (лок-4) — **Зеркала**: двойники ВНЕШНЕ идентичны; тэлл по поведению — фантомы зеркалят движение игрока, настоящая охотится по-настоящему и белеет от попаданий.
8. **Мёртвый король** (лок-5, финал) — крутой мобильный дуэлянт, **бессмертен**. В центре зала трон, вокруг факелы; подходишь → факел зажигается, гаснет через N сек; горят ВСЕ одновременно → король смертен. При появлении короля все факелы тухнут (обучающий момент).

**Пилот:** первым делаем **Матерь роя** целиком (спрайт + механика), тестим, по ней решаем темп остальных.
Боссы рисуем в **текущем пиксель-пайплайне** (крупнее/детальнее), не внешними ассетами.

### 3. 0.31 «Орда гуще» (часть арки)
- 20+ простых, но визуально разных врагов (рефактор поведения в `behavior`; фикс белых версий — перебор `ENEMY_MAPS`).
- **Конечные тематические волны:** фон = тонкий трикл дешёвого мяса; опасные/спец-типы приходят
  **конечными группами** (перебил — угроза снята). **Ни один опасный тип не идёт вечно** → выгодно бить тех, кто мешает.

### 4. Экономика — усложнить (ПОСЛЕ боссов и контента)
- Не просто поднять цены, а **уменьшить выпас монет**: каждая монета ценнее, зарабатывать сложнее.
- **Дроп монет зависит от врага:** базовое мясо почти/не даёт монет; сложнее/крупнее враг → выше шанс и
  сумма; боссы — крупные выплаты. Дороговизна мета-прокачки через дефицит, а не инфляцию цен.

### 5. Панель разработчика (вынесена ВПЕРЁД — в фундамент первого захода, решение 16.06.2026)
- Открытие: секретный жест — N быстрых тапов по плашке версии `#verCorner` (незаметно).
- Разделы: старт любой локации/героя; заспавнить любого босса + «след. фаза»/«убить»; выдать любое
  орудие/пассивку на уровень, «всё максом», форс эволюции; задать уровень/опыт/золото; warp по времени +
  таймскейл (слоумо/перемотка); бог-мод/неуязвимость/спавн N врагов; оверлеи хитбоксов/зон/FPS.
- Реализация: визуальная обёртка над существующим `window.__game` + пара новых хелперов. Скрыта в проде за жестом.

### 6. Оптимизация под слабые устройства (В САМЫЙ КОНЕЦ)
- Кандидаты: пулинг объектов (пули/кристаллы/партиклы — без аллокаций в кадре); адаптивные каппы
  партиклов/врагов по железу; кламп DPR/разрешения канваса на слабых; динамическая деградация качества по
  FPS; сократить овердро (торч-свет/виньетка). Профилировать на реальном слабом девайсе.
- **Спавнить врагов БЛИЖЕ к зоне видимости (идея владельца).** Сейчас спавн-радиус слишком большой (`spawnBoss/director`
  считают от `Math.max(W,H)*…`), мобы рождаются далеко за экраном → лишние сущности обновляются/рисуются впустую.
  Уменьшить дистанцию спавна (чуть за краем экрана). ⚠️ **ВАЖНО:** враги НЕ должны «исчезать», если игрок убежал далеко
  от них — уже заспавненные живут (никакого деспавна по дистанции), сокращаем только радиус ПОЯВЛЕНИЯ новых.
- **Опция «упрощённая анимация мобов» в настройках (идея владельца).** Тумблер, который отключает 2-й кадр ходьбы
  (и/или прочую анимацию врагов) ради FPS на слабых устройствах. Реализация: флаг в `meta`/настройках → в `drawEnemy`
  не переключать кадр (`stepRows`), рисовать статичный спрайт. Хорошо ложится в общий «режим производительности».

### ➕ Дополнительные правки (добавлено 16.06.2026; порядок гибкий)
- ~~**🏷️ ПЕРЕИМЕНОВАНИЕ «Long Night» → «Nox Atra» + пасхалка-перевод**~~ — ❌ **ОТМЕНЕНО** (16.06.2026). Владелец решил оставить **«LONG NIGHT»**
  (меню на 0.38 отлично смотрится именно с ним, название уже на английском — пасхалка-перевод теряла смысл). Не делать.
- **Пиксельный UI (по эскизу владельца).** Переделать меню и экраны в пиксель-арт стиль игры.
  ⚠️ **ИЗМЕНЕНИЕ ПРИНЦИПА:** раньше UI был «чистый/читаемый», теперь — пиксельный под мир. Владелец готовит
  эскиз в Claude Design (меню) и пришлёт скрин (+ опц. код-референс). Реализация — в существующем DOM+CSS
  (UI остаётся DOM/CSS ради перфоманса; мир — канвас), пиксельные рамки/иконки как CSS или спрайты;
  мобильность/PWA сохранить. Делаем, когда придёт эскиз.
- **Сброс прогресса в настройках.** Кнопка «Сбросить прогресс» с защитой от случайного нажатия: рядом
  счётчик **×5**, каждое нажатие ×5→×4→…→×1, следующее = полный сброс (meta/localStorage → defaultMeta).
  Быстро прокликать 5 раз = сброс. Малое, можно в ближайший полиш.
- **Стиль боссов — сдвинуть в сторону пиксель-арта.** Матерь роя (0.33) сделана гладкой (градиенты/кривые);
  владелец хочет хотя бы немного сместить боссов к пиксельному виду игры, сохранив качество анимации.
  Поэкспериментировать: квантизация/пиксельные текстуры тела, более «ступенчатые» формы, пиксельные глаза/детали.
- **Повысить сложность — ПОД ВОПРОСОМ** (нужно подтверждение владельца). Игра казалась лёгкой, возможно, потому
  что владелец был всегда полностью прокачен (добавлен сброс прогресса в 0.36). Решить ПОСЛЕ теста с нуля; если
  нужно — поднять HP/урон/скорость/плотность врагов и агрессивность `director` (на этапе экономики).
- **Музыка по локациям → ПЕРЕНЕСЕНА В САМЫЙ КОНЕЦ ROADMAP** (решение владельца 16.06.2026). См. раздел **«🎵 МУЗЫКА — В САМОМ КОНЦЕ»** внизу файла.
- **Правки по интерфейсу (0.35):** шрифт VT323 встроить base64 (работает на `file://`), мыши — медленный плавный взмах,
  мрачные облака, золото «приклеено» к кнопке «СКЛЕП», подпись музыки вернуть очень мелкой.
- **Геймплейные правки (0.35):** меньше сквош-анимации мобов, фонарь уже на старте (финал по ширине как сейчас),
  маленький стартовый радиус сбора (`magBase`).

## Как тестировать (headless)
- Локальный сервер: `python3 -m http.server 8773` в папке игры.
- Headless Chrome с CDP: запускать **обязательно с `--mute-audio`** и убивать после тестов
  (`pkill -f "remote-debugging-port=9222"`). Иначе оставленный процесс играет музыку «из ниоткуда».
- Управление через playwright MCP (navigate/evaluate/screenshot).
- В игре есть debug-API `window.__game`: `start() spawn(type,n) boss(tier) warp(sec) god(b)
  kill() levelup() pick(i) count() fps() meta()`.
- DPR-баг был: на Retina нужен и `cv.width=W*DPR`, и `cv.style.width=W+'px'` (исправлено).

## Деплой
```
git clone https://github.com/emorozoff/long-night.git /tmp/crimson-deploy
cp game.html /tmp/crimson-deploy/index.html   # + музыка в /tmp/crimson-deploy/music/ когда будет
cd /tmp/crimson-deploy && git add -A && git commit -m "..." && git push
```
GitHub-токен лежит в macOS keychain (git credential-osxkeychain, аккаунт emorozoff). gh CLI нет.

## Архитектура game.html (секции по порядку)
utils → audio (объект `A`, синтез SFX + процедурная музыка) → meta (defaultMeta/sanitizeMeta,
localStorage) → LOCS (локации) → input (+ touch-джойстик) → sprites (`buildSprites`,
пре-рендер в offscreen-канвасы) → run state (`newRun`) → UPGRADES (пул апгрейдов) → combat →
weapons (`fireWeapons`) → enemies → boss → director (спавн/сложность) → zones (лужи/разломы) →
update → render → ui/screens (`setState`, `SCREEN_MAP`) → wiring → debug API → boot.

Состояние забега — объект `run`, пересоздаётся в `newRun()`. Глобальный `state`:
menu/shop/settings/playing/paused/levelup/dead.

## Контент сейчас
- **6 орудий:** Арбалет(bolt), Серебряные клинки(orbit), Небесная кара(chain),
  Освящённая земля(aura, ещё и рассеивает вражеские снаряды), Метательный крест(scythe, бумеранг),
  Святая вода(mortar, лужа огня). Лимит — 4 орудия за забег.
- **12 пассивок + 7 синергий** (см. массив UPGRADES, id'шники в скобках выше по тексту).
- **7 типов врагов** (zombie/bat/slime/spitter/brute/warper + boss), боссы каждые 3 мин с фазами.
- **3 локации:** Погост (grave), Кровавая топь (swamp, лужи замедляют, +15% золота, откр. рекорд ≥4:00),
  Пепелище (ash, огненные разломы, +25% золота, откр. рекорд ≥7:00).
- Мета: золото → «Склеп» (8 перманентных улучшений), экспорт/импорт сейва JSON.

## ⏳ ОЧЕРЕДЬ ПРАВОК (всё ниже КРОМЕ врагов сделано в 0.28; осталось: враги → 0.29)
- **Заголовок «LONG NIGHT» — кроваво-красный.** Сейчас (0.27) цвет #c7545f читается как розовый.
  Нужен насыщенный цвет крови (тёмно-алый, напр. ~#a01020 / #b3121f, не розовый, но и не кислотный
  неон как был в #d4203a). Глубокий «кровь», не пастель. Менять `.title` color + titlepulse glow.
- **Капающая кровь с заголовка** (эксперимент): попробовать анимацию стекающих капель крови с букв
  LONG NIGHT (CSS-капли/псевдоэлементы или canvas-капли в drawMenuAmbient под заголовком). Если выйдет
  аккуратно — оставить; это «давай попробуем».
- **Луна в меню — ярче и очевиднее (слегка).** В 0.27 обесцветил до бледно-серой (#2c2c34 + слабый
  серый glow в `drawMenuAmbient`) — теперь её почти не видно/непонятно что это. Сделать заметнее:
  чуть светлее диск + более явное (но не кислотное) свечение, можно лёгкий холодный оттенок, чтобы
  читалась как луна. Не возвращать кровавый красный — контраст фон/UI сохранить.
- **Увеличить весь левый-верхний HUD-кластер.** Крупнее: полоска HP (#hpbar/#hpText), строка
  УР./☠ убийства/◆ золото (.hudrow), иконки предметов (#hudItems/.ichip — сейчас 44px, ещё крупнее),
  и вообще весь блок hudTL. Пропорционально увеличить шрифты/отступы. Проверить, чтобы не налезало на
  таймер и не ломалось на мобиле.
- **HP-бар и XP-бар — моментальные, без плавности.** Сейчас у #hpFill (transition width .12s) и #xpFill
  (.15s) — плавная анимация. Убрать transition, чтобы реагировали мгновенно/отзывчиво.
- **Снаряды — полностью пиксельные, с нуля.** Переделать вид ВСЕХ снарядов в пиксель-стиль (как враги),
  чтобы форма читалась по задумке: болт «Арбалета» — стрела/болт; орбитальные «Серебряные клинки» —
  похожи на клинки; «Метательный крест» — крест; вражеские снаряды (eb) — сгустки/шары своей стихии;
  цепная молния — пиксельный разряд; святая вода (горшок) — склянка. Сделать спрайты `px()` и рисовать
  их в drawBullets/drawBlades вместо текущих кругов/линий. Постараться, чтобы выглядели аккуратно.
- **Кнопка «на весь экран».** requestFullscreen (документ/боди) по кнопке — в настройках и/или
  иконкой в меню. Учесть, что нужно по жесту пользователя.
- **HUD: разделить оружие и предметы + видимые слоты оружия.** Иконки оружия отдельной группой,
  пассивки/синергии — отдельной. Показывать ВСЕ слоты оружия (лимит 4) включая ПУСТЫЕ, чтобы игрок
  сразу видел, что все орудия не собрать. (см. скрин владельца)
- **Больше врагов — минимум 20 типов (0.29).** ВАЖНО (уточнение владельца): враги должны быть
  ПРОСТЫЕ, но РАЗНЫЕ по виду/HP/урону/скорости/размеру — как в оригинальном Vampire Survivors (много
  визуально разных, но без хитрых механик). НЕ делать сложных (никаких некромантов, «бей со спины»,
  каменных башен). Реализация: рефактор поведения в поле `behavior` (chase/fast/swarm/ranged/split/
  exploder), большинство новых = просто chase с уникальным спрайтом и статами. Добрать до 20+ типов.
  Владелец отметил по вкусу: Скелет-латник(танк-chase), Адская гончая(fast), Зомби-бомба(exploder),
  Скелет-лучник(ranged как spitter), Паук(split как slime), Рой мошкары(tiny swarm) — БЕЗ некроманта.
  Плюс добрать простыми вариантами: гуль, скелет, культист, утопленник, личинка, огр, призрак и т.п.
  У каждого: пиксель-спрайт px(), стат-веса по локациям, запись в бестиарий/ENEMY_INFO.

### План реализации
- **0.28 «Интерфейс и бой»:** кровавый заголовок + капли, ярче луна, крупнее весь левый HUD
  с разделением оружие/предметы и видимыми слотами оружия, мгновенные HP/XP бары, пиксельные
  снаряды с нуля, кнопка «на весь экран».
- **0.29 «Орда гуще»:** выбранные владельцем новые враги (до 20+ типов).

## Журнал версий
### 0.87 — Заход 21: мобильный Склеп (один скролл, перма-карточки 2 колонки квадратные)
- Владелец (скрин): в Складе ДВА внутренних скролла (`#shopGrid` 46vh + `#shopWGrid` 34vh). Просьба: перма-карточки меньше/квадратные, обе секции развёрнуты, ОДИН скролл на всё окно.
- Мобильный `@media`: `#scrShop{justify-content:flex-start}` (экран скроллится от верха), `#shopGrid`/`#shopWGrid` → `max-height:none;overflow:visible` (внутренние скроллы убраны → один скролл на весь экран). `#shopGrid` → 2 колонки; `.mcard` `min-height:150px`+flex-центрирование+`text-align:center` → карточки 171×150 (ratio 1.14 ≈ квадрат). Компактные шрифты `.mname/.mdesc/.mlv/.mcost`. Всё scoped к `#shopGrid`/`#scrShop`.
- Проверено@390px: screen_scrolls=true, shopGrid/shopWGrid_scrolls=false (ровно ОДИН скролл); 9 орудий развёрнуты, НАЗАД внизу. Десктоп не тронут. sw CACHE→0.87.

### 0.86 — Заход 20: мобильные карточки героев (2 колонки, ≈квадратные, крупные иллюстрации)
- Владелец (скрин): на мобиле карточки героев во всю ширину/высокие, иконки крошечные. Просьба: меньше/квадратнее карточки + БОЛЬШЕ иллюстрации.
- Мобильный `@media`: `#heroGrid` → `grid-template-columns:repeat(2,1fr)` (2 колонки), `#heroGrid .hico` 22×27→**66×79** (крупно), компактные шрифты `.mname/.mdesc/.mcost`, padding 10/8. Всё scoped к `#heroGrid` (Склад/`.mcard` не задеты). Проверено@390px: 2 колонки по 171px, карточка 171×182 (ratio 0.94 ≈ квадрат), 4 героя видны без скролла.
- Десктоп не тронут (всё в media-query). Синтаксис не менялся (CSS). sw CACHE→0.86.

### 0.85 — Заход 19: прогрессия боссов/локаций (по победе) + один босс за раз + боссы слабее в начале
- Жалоба 1: локации открывались БЕЗ убийства босса (была разблокировка по лучшему ВРЕМЕНИ выживания `best<L.req`, req=сек). Жалоба 2: боссы спавнятся по таймеру каждые 180с и НАКАПЛИВАЮТСЯ, неубиваемы рано.
- **Локации — по победе:** `meta.locWon{}` (defaultMeta+sanitizeMeta). `winGame` ставит `meta.locWon[run.loc]=true`. `buildPickLoc` показывает локацию если она первая ИЛИ предыдущая (по порядку LOCS) пройдена. Старый time-gate убран. Проверено: пусто→только grave; locWon.grave→+swamp (не ash). ⚠️ У игрока теперь откроется только 1-я локация (остальные — за победу над боссом локации) — это и просили.
- **Один босс за раз:** в director gate `&& !bossAlive` (`run.bossRef&&!.dead`) + `run.bossT=t+BOSS_INTERVAL`. Следующий босс не придёт, пока жив текущий → НЕТ накопления. Не убил — новые не идут. Проверено: пока первый жив, на поле 1 босс даже спустя 5×интервал; после убийства приходит следующий.
- **Боссы слабее в начале:** `DIFFICULTY.warm(m)` теперь множит HP И урон боссов в spawnBoss (раньше warm на боссов не действовал). Первый босс 3:00: 1716→1316 HP, полная сила к 9:00. РУЧКА — если всё ещё не убить, резать `1100` базу/добавить boss-warm.
- Синтаксис OK, консоль чистая. sw CACHE→0.85.

### 0.84 — Заход 18: кристаллы опыта НЕ исчезают никогда (убран кап поля)
- Владелец: «кристаллы опыта не исчезают никогда». Лежащие кристаллы и так persist (нет таймера жизни; `sweep` чистит только собранные `dead`). Реальная потеря опыта была в `gemDrop`: при «полном поле» (`fieldXp>xpFieldCap()` ~3 ур.) новый кристалл НЕ ронялся → опыт терялся.
- Фикс: пейсинг-кап поля УБРАН — весь опыт всегда падает кристаллом. Осталась только перф-страховка на ЧИСЛО объектов (`xpCount>=300`, важно для iPhone), но теперь при переполнении опыт НЕ теряется — доливается в существующий кристалл (`g.v+=value`). Проверено: 350 дропов → 300 объектов, суммарный опыт 2100 (=350×6) сохранён полностью, none_lost=true.
- Лавина не вернётся: сбор пейсится брейком `if(state!=='playing') break` в updateGems (левел-ап открывается → сбор паузится). `xpFieldCap()/fieldXp()` остались как dead-code (не зовутся; не удалял на случай реверта).
- Синтаксис OK, консоль чистая. sw CACHE→0.84.

### 0.83 — Заход 17: −20% HP всем врагам + ramp сложности 6→9 мин (медленнее)
- **−20% HP всем:** `DIFFICULTY.enemyHp 0.5→0.4`. Зомби старт 5→4.
- **Ramp сложности растянут 6→9 мин:** `DIFFICULTY.warm` знаменатель `m/6→m/9` (0.65×→1× теперь к 9:00). Сложность растёт медленнее; середина мягче (зомби 6мин 113→80 — ×0.8 HP + warm 0.883). После 9 мин — полный baseline (как раньше после 6, но HP ×0.4). hpWarm(4мин) и coinWarm(6мин) НЕ трогал — владелец просил именно ramp сложности.
- Синтаксис OK, консоль чистая. sw CACHE→0.83.

### 0.82 — Заход 16: нерф гончих (вместо мышей) + старт −30% HP
- Уточнение владельца: «быстрая стая» = СВОРА ГОНЧИХ («свора мчится!»), не мыши. ETYPES `hound`: `dmg 13→6.5` (÷2), `spd 140→112` (−20%). Мыши (`bat`) ВОЗВРАЩЕНЫ как были (spd 128, dmg 5) — откат 0.81.
- **Старт −30% HP всем мобам:** новая ручка `DIFFICULTY.hpWarm(m)=0.7+0.3·min(1,m/4)` (0.7×→1× к 4:00, ТОЛЬКО HP, не урон), входит в hpMul. Проверено: зомби t0 7→5 HP, 4мин+ — без изменений (113 на 6мин как было).
- Синтаксис OK, консоль чистая. sw CACHE→0.82.

### 0.81 — Заход 15: нерф стаи летучих мышей (волна «стая окружает!»)
- Жалоба владельца: на ~2-2:30 приходит быстрая стая (волна `bats` в doWave — ring из 22 шт, баннер «стая окружает!»). Урон ÷2, скорость −20%.
- ETYPES `bat`: `dmg 5→2.5`, `spd 128→102` (стр.1318). Затрагивает волну `bats` + вороны Пастыря (noScale, приемлемо). Проверено: база 102/2.5; инстанс на 2:19 бьёт 3.56 (было 7.1).
- 🆕 ПРОЦЕСС: владелец просит ОТНЫНЕ всегда коммитить+пушить СРАЗУ после любого изменения (играет и правит баланс на ходу) — больше не ждать команды «коммить». sw CACHE→0.81.

### 0.80 — Заход 14: баланс (скорострельность арбалета ÷2, урон всего оружия −30%)
- **Арбалет ÷2 скорострельность:** кулдаун `wt.bolt` ×2 (стр.~2067). Проверено: lv1 0.92с→1.84с. No-target re-poll (0.15с) не трогал.
- **Урон всего оружия −30%:** новая ручка `WDMG=0.7` (рядом с WSET, стр.~1704), применяется в damageEnemy: `if(WSET.has(src)) dmg*=WDMG` — scoped к 9 орудиям (bolt/orbit/chain/aura/scythe/mortar/bell/lantern/sword). burn/bleed наследуют (их магнитуда считается от уже уменьшённого удара). Среда (rift/tide/grave), бомба-пикап, nova-возрождение, shatter-пассивка — НЕ тронуты. Проверено: урон оружия 100→34.9 (=×0.5 крипт × 0.7); rift 99.8 без изменений.
- Синтаксис OK, консоль чистая, рантайм через `__game`. sw CACHE→0.80.

### 0.79 — Заход 13: «тёплый старт» (монеты 2×→1× к 6 мин, сложность 0.65×→1× к 6 мин) + сложность по локации
- Жалоба владельца: старт слишком сложный, и не успеваешь заработать золото на прокачку Склепа. Решение — мягкий разгон первых 6 минут + прогрессия сложности по картам.
- **Монеты — тёплый старт:** `coinWarm=Math.max(1, 2 - run.time/360)` множит шанс дропа (killEnemy, стр.~1773). ×2 в 0:00 → ×1 к 6:00, дальше как было. Проверено: t0=2, 3мин=1.5, 6мин=1.
- **Сложность по времени — тёплый старт:** новая ручка `DIFFICULTY.warm(m)=0.65+0.35·min(1,m/6)` (0.65×→1× к 6:00). Входит в `emul=mul·warm(m)·locDiff`, применяется к `hp`/`dmg` в spawnEnemy. Поздняя игра НЕ меняется (warm=1 после 6 мин). Проверено: зомби t0 grave 11→7 HP / урон 9.6→6.24; t6 — полная кривая по времени (HP 113).
- **Сложность по локации (ответ на «каждый уровень сложнее» = ЛОКАЦИЯ):** поле `diff` в `LOCS` — grave 1.00 / swamp 1.12 / ash 1.25 / crypt 1.38 / inferno 1.50; кэш `run.locDiff` в newRun, входит в `emul`. Проверено: inferno/grave урон-ratio ровно 1.5×. Боссы не трогали (у них уже растёт тир по локации).
- Все множители — тунабл одной строкой. Синтаксис OK, консоль чистая, рантайм подтверждён через `__game`. sw CACHE→0.79.

### 0.78 — Заход 12: баланс (HP врагов ÷2, скорость игрока −30%, вампиризм 3%) + иконки героев ещё меньше
- **HP всех врагов ÷2:** новый тунабл-кноб `DIFFICULTY.enemyHp=0.5`, применён в `spawnEnemy` к `hp/maxhp` (покрывает scaled + noScale-фоддер + элиты ×9 + детей паука). Золото/опыт НЕ затронуты (дроп считается от `e.xp`, не от maxhp). Проверено рантайм: зомби 22→11, brute 180→90.
- **Скорость бега игрока −30%:** база `230 → 161` в расчёте `spd` (единственная ссылка; магнит остаётся 190px/с — кристаллы сами долетают).
- **Вампиризм (Чаша Грааля) 2%→3%:** аккумуляция `leechPool += dmg*0.03*leech` (было 0.02) + текст карточки «3% нанесённого урона лечит тебя». Скорость слива пула не менялась.
- **Иконки выбора героя ещё в 2 раза меньше** на мобиле (`.mcard .hico` 44→22px / 54→27px). Каскад уже исправлен в 0.77 — медиа-запрос в конце CSS перебивает базу; проверено getComputedStyle@380px = 22×27.
- Синтаксис OK, консоль чистая, рантайм-значения подтверждены через `__game`. sw CACHE→0.78.

### 0.77 — Заход 11: мобильные фиксы (тёмные края зума, HUD крупнее, иконки героев меньше)
- **Тёмные края при зуме ИСПРАВЛЕНЫ:** `drawGround` рисовал землю/декор/лужи на `W/2,H/2`, а при зуме видно `W/2/ZOOM` → края оставались тёмными (фон). Введён `vw=W/2/ZOOM, vh=H/2/ZOOM`, все 3 слоя (тайлы/лужи/декор) расширены до видимой зоны. На десктопе ZOOM=1 — без изменений. Проверено форс-зумом: земля до краёв.
- **Игровой HUD — крупнее (как раньше/0.75):** откат мобильного уменьшения HUD из Захода 10 (scale 0.7→0.84, шрифты/полосы назад). ⚠️ Найден и исправлен КАСКАД-БАГ: мобильный `@media` стоял ПЕРЕД базовыми правилами карточек/иконок → те его перебивали (иконки героя оставались 60px). Медиа-запрос перенесён в КОНЕЦ CSS (стандарт) — теперь перебивает базовые. Проверено: HUD scale 0.84, иконки героя 44px.
- **Иконки выбора героя — меньше** на мобиле (`.mcard .hico` 60→44px).
- Заголовок-фикс (центрирование) сохранён. Проверено скриншотами (герои-390/бой-зум), десктоп без регрессий, консоль чистая. sw CACHE→0.77.

### 0.76 — Заход 10: мобайл (зум/UI/монеты/кристаллы/заголовок) + стрелки ослаблены + ПРОКАЧКА ОРУЖИЯ в Складе
- **Мобайл (`MOBILE=_isTouch()`):** зум-аут камеры `ZOOM=0.78` (в `render` scale + все мировые радиусы спавна/деспавна на `/ZOOM`, стрелка к боссу с учётом зума) — видно больше поля; UI компактнее (CSS @media 640px); монеты −30% (`COIN_SC=1.5*0.7`); кристаллы опыта НЕ качаются (`bob=0` для xp); заголовок в меню выровнен по центру (text-indent компенсирует letter-spacing). На десктопе всё неактивно (ZOOM=1). Проверено: форс ZOOM=0.78 → мир мельче, враги не в кадре; заголовок центр на 375px.
- **Сохранение/Загрузка** перенесены из Склепа в Настройки (бэкап сохранён).
- **Стрелки** (spitter/archer/cultist, не боссы): снаряд −30% скорости (240→168) + +40% размер (r5→7, life↑); скорострельность −30% (atkT 2.4→3.1); вдвое меньше в волнах (doWave ×0.5). Проверено.
- **🔧 ПРОКАЧКА ОРУЖИЯ в Складе (тир 0-50, per-weapon):** `meta.wcrypt[id]`. Урон: `wcDmg(src)=0.5+0.03*t` (тир 0→50 = **×0.5→×2.0**) ЦЕНТРАЛЬНО в `damageEnemy` (только 9 оружий `WSET`, боссовые зоны/пикапы не задеты). Фирменный «охват» (Пакет B): `wcSig(id)=1+0.01*t` (×1.0→×1.5) на per-weapon стат (болт-пробитие/клинки-радиус/цепь-прыжки/кадило-радиус/крест-дальность/вода-охват/колокол-радиус/фонарь-дальность/меч-радиус). Внутриранговый левел-ап (карты lv1-5) ОСТАЁТСЯ. UI: секция «ПРОКАЧКА ОРУЖИЯ» в Складе (9 карточек: иконка/тир/прогресс-бар/«урон ×N · +охват ×N»/цена `2+floor(t*0.6)`). `meta.wcrypt` в default+sanitize (clamp 0-50). Проверено: t0 урон ×0.5, t50 ×2.0 (реальный 52→213, ×4.1), охват ×1.5, не-оружие ×1, покупка/санитайз ок.
- Проверено headless + скриншоты (Склеп/меню-375/бой-зум), консоль чистая. sw CACHE→0.76.
- **Состязательное ревью (3 агента, ultracode):** «реализовано корректно, блокеров нет». Применены 4 фикта: (1) тор-свет Короля учитывает ZOOM (был моб-рассинхрон свечения с факелами); (2) стартовое кольцо мобов /ZOOM; (3) запас отрисовки препятствий /ZOOM; (4) DoT горения src 'mortar'→'burn' (убран двойной wcDmg + независимость от тира воды; заодно лок-огонь ash/inferno больше не зависит от прокачки воды). Перепроверено. ⚠️ **Заметка владельцу (плейтест):** база ×0.5 (тир 0) + Заход 9 (×1.5 скорость, ×1.2 HP врагов) = ранний забег с НУЛЯ заметно жёстче (старый урон возвращается к тиру ~17). Это намеренно, но проверь ощущения; крутить через `wcDmg`/`DIFFICULTY`. Ниты (плейтест): wcSig на дискретных статах (болт-пробитие/цепь-прыжки) растёт скачком только к высоким тирам. **НЕ закоммичено — ждёт команды.**

### 0.75 — Заход 9: скорость ×1.5 + единый регулятор сложности (+20%) + синергия-чипы + авто-курсор
- **Скорость ВСЕЙ игры ×1.5** (`GAMESPEED`, в `loop` множит dt симулятора; всё ускоряется вместе → баланс сохранён). Кламп `Math.min(dt,0.05/GAMESPEED)` перед ×1.5 → эфф. макс-шаг 0.05 (анти-туннель на низком FPS; 1.5× держится до 30fps). Проверено: run.time идёт ×1.5.
- **Единый регулятор сложности** `DIFFICULTY{mul,hp(m),dmg(m),spd(m),bossHp(m),bossDmg(m)}` — крутить ТОЛЬКО здесь (для будущих правок «легко в начале / ад на 10-й мин» = переформировать кривые). Сейчас **mul=1.2 (+20%)** к HP/урону врагов И боссов; скорость врага без mul (игра и так ускорена). Густота спавна (diffNow) НЕ тронута. Проверено: зомби HP 18→22, урон 8→9.6; босс tier1 1100→1320.
- **Синергия-чипы на карточке оружия** (левел-ап): пиксельные чипы «⮂ [слияние]» (фиолетовый, из `FUSIONS`) + «★ [эволюция]» (золотой, из `EVOS.partner`) с иконкой партнёра. В `EVOS` добавлено поле `partner` (id пассивки). `weaponSynergies()` + `renderChoices` + CSS. Проверено скриншотом.
- **Курсор авто-скрытие:** видим обычно, прячется через 1с без движения мыши, появляется при движении (`body.ingame.idle` + mousemove-хендлер). На тач — без изменений.
- Состязательное ревью (1 агент, фокус на ускорении/сложности): «безопасно, багов нет, dt-консистентно, формулы ×1.2 эквивалентны»; применён анти-туннель кламп. Консоль чистая. sw CACHE→0.75. **НЕ закоммичено — ждёт команды.**

### 0.74 — Заход 8C: полный реворк достижений (прогресс-бары + иконки + live + всплывашки)
- Структура ачивок: `{id,name,desc,icon,cur:m=>число,goal}` (вместо bool `check`). **27 достижений** (убийства/боссы/время/победы/комбо/герои/локации/бестиарий/уровень/оружие/эво/фьюз/золото/Склеп/без-урона). `achProg(a)`={cur,goal,pct,done}.
- **Пиксельные иконки** `ACH_ICONS` (15 px-канвасов: череп/корона/луна/солнце/кровь/медаль/часы/звезда/книга/меч/искра/слияние/монета/карта/щит) + `achIcoURL`.
- **Карточки в Летописи:** иконка + название (Tiny5) + описание + **прогресс-бар** + % + «cur / goal», **сортировка по %** (готовые/почти готовые вверху). Пиксельная рамка (золотая у открытых).
- **Live-разблокировка В БОЮ:** `metaLive()` (мета + прогресс текущего забега) + `checkAchievementsLive()` (троттл 1с в `update()`). **Всплывающая карточка** `achPop` (#achPops, вплыв справа, авто-увод 3.6с, кап 4).
- **Новые счётчики `meta.stats`:** goldTotal (лайфтайм золото), bestLevel, evos, fusions, weaponMaxed, noHitBest — подключены (recordRun / applyUpgrade / damagePlayer+update / 3 места золота). Дефолт+санитайз обновлены.
- Золото за ачивки убрано (0.73). Проверено headless: 27 ачивок, иконки, прогресс 50% (5000/10000), live-разблокировка (kill1k/evo/fusion в бою), счётчики, сортировка, золото не капает, попапы создаются (топовый элемент по elementFromPoint), забег без крашей, консоль чистая. sw CACHE→0.74.
- **Состязательное ревью (3 агента, ultracode):** вердикт «крепко, блокеров нет, крашей/утечек/двойного-счёта/TDZ нет». Применены 3 фикса: (1) «Богач»/«Скупщик»/«Орден» открываются вовремя (скан ПОСЛЕ прибавки золота в gameOver/winGame/quitToMenu + после покупок в Складе/у героев — раньше был скан до прибавки → задержка на забег); (2) `weaponMaxed` `>=5`→`===5` (Арбалет max:6 двоил счётчик); (3) «Коллекционер» off-by-one (босс учитывался дважды). Перепроверено. **НЕ закоммичено — ждёт команды.**

### 0.73 — убрано золото за достижения (досрочно из 8C)
- Владелец заметил, что доход за ачивки не убран (он в 8C, а делали 8A/8B). Убрано сразу как тривиальная правка: `checkAchievements` больше не делает `meta.gold+=a.gold` (и баннер без «+◆»); в Летописи (`buildStats`) ценник «◆N» заменён на ✓ для открытых. Ачивки по-прежнему открываются. Проверено: золото не меняется при разблокировке, ценник пропал. ОСТАЛОСЬ в 8C: прогресс-бары + иконки + пиксельные карточки + сортировка по % + всплывашки в бою. sw CACHE→0.73.

### 0.72 — Заход 8B: реворк механик 4 боссов (Палач/Утопленница/Пастырь/Король)
- **Гнилой палач:** лужи теперь ЖИДКИЕ (волнистый блоб, не круг) + КРУПНЕЕ (poolR [58,70,84]→[80,96,114]); НОВОЕ — **бросок слизи** в текущую точку игрока (`run.flaySlimes`, дуга-полёт → лужа ПОД игроком, гонит с места); **отравление 2с** после контакта с гнилью/слизью (`run.poisonT`/`poisonDps`, тик вне лужи). Проверено: лужи r=80 блоб, слизь в полёте, яд ~1.97с.
- **Утопленница — РЕВОРК:** убраны расходящиеся кольца-приливы. НОВОЕ — **лавина** (`spawnAvalanche`): фронт-полоса зелёной пиксель-воды из-за края экрана идёт К боссу сквозь игрока и СИЛЬНО толкает его К Утопленнице (push 210–300). Вокруг тела — **зелёные лужи** (урон+замедление). Проверено: толчок к боссу (263→54px), лужи с уроном, волна линейная, зелёный пиксель-рендер.
- **Тёмный пастырь:** A **СТАМПИД** — сгоняет имеющуюся орду к себе (импульс) → через 0.9с ГОНИТ её тараном на игрока (`o.stampT`, +320 к игроку; в тесте 29 мобов). C **ВОРОНИЙ ТУМАН** (`run.crowFog`) — тёмные зоны выклёвывают (DoT) + темнят обзор. Снаряды УРЕЗАНЫ по частоте (crowCd/arcCd выше).
- **Мёртвый король:** убран «типичный дэш с красной подсветкой» (chT/dashT) — только погоня шагом. B **ТЬМА** (`run.kingDark`) — гасит мир, виден круг вокруг героя + горящие факелы. C **КОСТЯНОЙ ФЕНИКС** — на ≤20% HP разово снова неуязвим, факелы гаснут (зажги заново). D **УКАЗ КОРОЛЯ** (`run.kingSpikes`/`kingDecree`) — лучи костяных шипов узором по полу (телеграф→удар). Проверено: дэша нет, тьма, 24 шипа, феникс-реинвулн.
- Правило соблюдено: ВСЕ мобы от боссов = 0 опыта. Новые поля в `newRun`, все апдейтеры вызываются в `update()`. Проверено headless + скриншоты каждого босса, консоль чистая, FPS ок. sw CACHE→0.72.
- **Состязательное ревью (4 агента, ultracode):** вердикт «логически корректно, крашей/утечек нет, не блокеры». Применены 3 фикса из находок: (1) стампид — драйв ПОВЕРХ позиции (не затирает нокбэк оружия); (2) лавина — быстрее (760+) и дольше живёт (не промахивается по быстрому игроку, бегущему к боссу); (3) туман/шипы гаснут при смерти своего босса (как волны — нет урона от мёртвого босса). Перепроверено. Заметки владельцу по плейтесту (без кода): сила толчка лавины (×3 в фазе 2), перекрывающиеся DoT не суммируются (общий i-frame 0.18с — защищает игрока), шипы могут быть невидимы во время тьмы (но и далеко от игрока). **НЕ закоммичено — ждёт команды.**

### 0.71 — Заход 8A: полиш (монеты/фонарь/могильщик) + лужи-озёра + UI (музтест/слайдеры/стрелка/бестиарий)
- **Монеты (вариант C):** свой МАЛЫЙ радиус притяжки (52px вместо общего magR) + резкое УСКОРЕНИЕ (`g.ms` 80→…→820) — монета «дзынькает» в руку, не ползёт издалека. `updateGems`. Проверено: дальняя (120px) не цепляется, ближняя ускоряется 80→113→147→180 и собрана.
- **Фонарь:** базовый конус — поворот **1500°/с** (`TAU*(1500/360)`, было 1080°), и **гаснет**, если в радиусе нет цели (флаг `run.lanternOn`, рендер конуса под гейтом). Синергия-луч (spear) — без изменений (360°/с). Проверено: нет цели→`lanternOn=false`, есть→`true`.
- **Могильщик:** поднятых зомби ×1.5 — `[5,7,9]`→**`[8,11,14]`** по фазам. Опыт 0 (правило). Проверено: фаза 0 → 8 поднятых, у всех `xp=0`.
- **Лужи уровней:** размер per-локация — `poolCell()`/`poolMul()`: **grave = «озёра» ×4** (сетка 620, r~240–460), swamp/crypt = ×2 (r~120–230), огненные без изменений. Замедление игрока: **grave ×0.62, swamp ×0.72, crypt — нет** (лёд тормозит нечисть, не тебя). Враги — как были. Проверено: размеры/факторы по локациям + видны крупные озёра в grave.
- **UI (вся зона теперь моя — UI-агент в отставке):** U1 убрана секция «Прослушать музыку» из настроек (+`buildMusicTest`/слушатель/вызовы); U2 пиксельные слайдеры громкости (квадратный трек/ползунок, красная заливка, проценты читаемы); U3 крупнее стрелка ▸→**▶** на «НА ОХОТУ»; U4 бестиарий — пиксельные карточки (резкая рамка, заголовки Tiny5, описания читаемы). Проверено скриншотами: меню/настройки/бестиарий/бой, консоль чистая, FPS 70. sw CACHE→0.71.
- **Состязательное ревью диффа (3 агента, ultracode):** вердикт «безопасно, реальных багов нет». Применены 2 гигиенические правки: `lanternOn:false` в `newRun` (инициализация); запас отрисовки луж 1→2 ячейки (без поп-ина краёв озёр grave). Заметки владельцу (без кода): (а) форма воды в рендере шире хитбокса замедления (намеренная развязка, на grave заметнее) — по плейтесту; (б) в grave-воде игрок (×0.62) медленнее врагов (×0.8) = вода в минус игроку (как и просил «тормозить сильнее») — подтвердить по ощущениям.
- **Чистка мёртвого кода (по команде владельца):** удалены осиротевшие методы/поля превью из объекта `MUSIC` (`play`/`stopPreview`/`preview`/`previewId`/`probeStatus`/`onStatus`/`onPrevEnd`/`loadAll`/`labels`/`order` + ветка превью в `tick()` + probe-машинерия в `deck()`). Движок воспроизведения (деки/кроссфейд/роли) не тронут — проверено: `init/tick/pauseAll` работают, роль в бою `calm`, консоль чистая. **ЗАКОММИЧЕНО (0.71).**

### 0.70 — мерж: визуал боссов (UI-агент) + мобильная оптимизация
- Слиты `ui-restyle` (0.69: анимация/детализация всех 8 боссов `BOSS_DRAW`, появление из земли) и `main` (0.60: мобильная оптимизация). Конфликтов кода НЕ было (зоны не пересеклись) — только VERSION/sw CACHE → 0.70. В итоге: её визуал боссов + мой геймплей (Заходы 6-7, фикс лавины опыта) + мобильные фиксы (авто-перф/вайт-аут/fmtNum) — всё вместе.

### 0.60 — мобильная оптимизация (аудит + фиксы)
- **Авто-перф:** рантайм `_lowperf` (вкл при FPS<38, выкл >52) → упрощённая анимация мобов + урезанные партиклы авто-включаются на слабых устройствах; **мобильный дефолт перф** (`_isTouch()` — тач-устройство стартует в перф-режиме).
- **Вайт-аут устранён:** area-оружие (Кадило/Колокол) больше НЕ красит всех врагов в белый (`e.flash` только для не-area) — на мобиле плотный бой с аурой забивал экран белыми вспышками, закрывая игрока. Подтверждено: после удара аурой по 148 врагам — 0 мигающих.
- **`fmtNum` в Летописи** «ЛУЧШИЙ УРОН ОРУЖИЕМ» (сырое 1029480773 → «1млрд»).
- **Адаптивный кап цифр урона** (36 вместо 90 на слабых/мобиле/перф).
- Аудит всех экранов на 375×812: меню/герои/склеп/летопись/бестиарий/настройки/HUD/левел-ап/смерть — ОК (адаптив UI-агента хорош). Отдано UI-агенту: компактнее карточки списков на мобиле (много скролла), перенос строки в настройках, высота карт левел-апа.

### 0.59 — Заход 7: ЧЕСТНЫЙ фикс лавины опыта + оптимизация
- **🎯 ЛАВИНА ОПЫТА ВЫЛЕЧЕНА ПО-НАСТОЯЩЕМУ (корень найден, не заплатка).** Корень: в `updateGems` не было `if(g.dead) continue`, а `sweep(run.gems)` пропускался при открытии левел-апа (`update()` выходит раньше) → собранный (мёртвый) кристалл оставался в массиве и **собирался снова каждый кадр** (`addXP` повторно) → каскад уровней. Воспроизведено (один кристалл v=50 пере-собирался: xp 7→33). **Фикс:** `if(g.dead) continue` в начале цикла `updateGems`. **Заплатка `lvlQueue≤3` УБРАНА.** Доказано: сбор полного поля → `run.xp` ТОЧНО = содержимому (ratio 1.000), нулевая инфляция.
- **Оптимизация:** (1) адаптивный капп партиклов по FPS (`_fps` бегущий; при FPS<35 партиклы режутся, при <32 кап 180) — авто-защита слабых устройств; (2) перф-режим отключает 2-й кадр анимации мобов (статичный спрайт). Спавн-радиус проверен — уже впритык за экраном (`hypot/2`), далёкие мобы телепортируются вперёд (не деспавнятся) → идея «ближе без деспавна» уже реализована.

### 0.58 — Заход 6: баланс оружия + экономика монет + музыка
- **Оружие:** Метательный крест кол-во ÷2; Святая вода (lv1=1 лужа, частота −20%); Фонарь (базовый конус поворот 1080°/с, луч-синергия 360°/с — разделены); Меч веры (глоб. кулдаун удара ~0.25с на меч + нокбэк 130→40 — не косит кучу мгновенно/не раскидывает).
- **Экономика монет ПЕРЕСОБРАНА:** золото за забег = ТОЛЬКО собранные с пола монеты (×goldMul). Убрана авто-формула (`kills×0.3+time×0.4+боссы×60+элиты×12+800`) и все авто-начисления (боссовое +40, сундук +15, level-up +15/+6 — HP оставлено). Дроп: коэффициент по силе моба `(0.004+xp·0.0035)·COINK` → кубик → РОВНО 1 монета (value 1). `COINK=1.4` откалибровано эмпирически (~13 дропов / ~10 собрано за ~770 killов ≈ 2 мин). Ручка тюнинга — `COINK`.
- **Музыка:** пауза→настройки = играет ИГРОВОЙ трек (меню-трек только на главном экране). Треки сжаты ещё ×2 (48k): 8.2 → **4.1 МБ**.
- **Сброс прогресса** сохраняет настройки графики/звука.

### 0.57 — мерж: боссы (механика) + UI-детализация
- Слиты `main` (боссы #2–#8, механика) и `ui-restyle` (детализация всех 21 врага, редизайн `BOSS_DRAW.matron`, экраны героя/локаций, пиксельные шрифты/читаемость, пульсация заголовка). Конфликтов не было (боссовый код и UI — в разных местах; версии совпадали). Дальше UI-агент делает `BOSS_DRAW.<kind>` для 6 новых боссов.

### 0.56 — Боссы #2–#8 (механика; один большой заход — параллельно UI-ветка дошла до 0.55)
- Реализованы ВСЕ оставшиеся 6 боссов (механики + функциональные спрайты; детализацию/анимацию — UI-агенту):
  **#3 Гнилой палач** — ядовитые лужи (area-denial, `run.flayPools`, DoT+вязнет); **#4 Утопленница** — прилив (катящиеся волны-телеграфы + замедляющая вода + миньоны 0-опыта); **#5 Тёмный пастырь** — вороны-снаряды (`ebAdd`: линия/дуга/кольцо) + наскоки + эскорт; **#6 Могильщик** — поднятие мёртвых (0 опыта, кап 150) + могилы-ловушки; **#7 Бледная дева** — зеркальные двойники (фантомы зеркалят ввод игрока, тель по поведению; HP-бар у фантома скрыт; гейт в `killEnemy`); **#8 Мёртвый король** (финал) — неуязвим, пока не зажжёшь 6 факелов (`run.torches` вокруг игрока, кольцо R=200, горят 12с; гейт в `damageEnemy`, дев-удар проходит).
- Процесс: спроектировано параллельным воркфлоу (6 агентов на реальном коде) → реализовано инлайн → проверено в браузере (все механики дают эффект, гейты работают, спрайты рендерятся) → адверсариальный ревью (7 агентов) → исправлено 5 major + 6 minor (очистка волн/факелов при смерти, перф forNear, перехват bossRef фантомом, центр/радиус/срок факелов, скрытие HP-бара фантома и др.). Консоль чистая.
- Переиспользованы существующие системы (зоны/лужи, `ebAdd`, `spawnEnemy` 0-опыта, dash-телеграф). Новое: `run.flayPools/tides/tideZones/torches`, гейт неуязвимости и гейт фантома.

**В этом же заходе — Босс #2 «Людоед» (maneater):**
- Новый босс (2-й из 8): `BOSS_MECH.maneater` + спрайт `SPR.boss_maneater` (клыкастый громила-пасть, рост виден через масштаб по `e.r`).
- Механика: агрессивная погоня + телеграф-рывок (×1.5 контакт в рывке); **ПОЖИРАЕТ ближайшего моба** (≈10/с, НЕ через `killEnemy` → игроку 0 опыта/дропа/комбо) → крепнет: +0.8% HP, размер 42→64, урон ×1.012/моб; **кап 50 съеденных** (насытился → перестаёт есть, добыча снова игроку).
- **Голодный рывок** (контр-кайт): пока недокормлен — иногда прёт В ближайшую стаю. **Фаза 2 (<25% HP): ЯРОСТЬ** — урон ×1.2, агрессия выше (быстрее/дальше рывки), жор-припадок (заглатывает до 6 разом). **Визуал «глоток»** — сплющивание тела при поедании (`e.chomp`).
- Прошёл адверсариальный ревью (3 ревьюера): исправлены троттл eatCd, кап-добыча, радиус forNear, косметичная фаза-2; признан корректным/сбалансированным/чисто интегрированным. Спрайт — функциональный, детализацию/анимацию (`BOSS_DRAW.maneater`) позже отдать UI-агенту.

### 0.51 — мерж: 4 модели героев в main
- Fast-forward `ui-restyle`(cf23910) в `main`: моё 0.49 цело + UI-агента детализация героя. Версия итога 0.51, sw CACHE → 0.51.

### 0.50 — 4 разные модели героев в бою (UI-агент)
- `HEROSPR` (hunter/paladin/warlock/inquisitor) — свой спрайт+анимация ходьбы; `drawPlayer` выбирает по `run.hero`.

### 0.49 — Заход 5: экономика + буф Матери роя + музыка
- **Экономика:** монеты выпадают ×20 реже (`cc/20`), цены Склепа ÷10 (`metaCost /10`), монета ×1.5 визуально, новый SFX `A.coin()` (звон при подборе).
- **Матерь роя сильнее:** скорость +40% (×1.4), мошки ×3 (выброс 12/18/27, потолок 70→210, фаза-2 выброс 18→54).
- **🎵 Музыка:** 5 собственных треков владельца сжаты ffmpeg 96k (18→8.8 МБ) в `music/track1..5.mp3`. `MUSIC` переписан по ролям
  (меню=t1, уровни=t2/t3, босс=t4, опасность/финал=t5). **Бесшовный кроссфейд-луп** (2 `<audio>`/дека, `applyDeck`).
  **Меню прослушивания** в настройках (статус загрузки ●, ▶/■, `buildMusicTest`). Атрибуция убрана.

### 0.48 — мерж UI-ветки (UI-агент)
- Иконки героев, курсор (скрыт+пиксельный), монетный чип с кнопкой, фактура/рельеф/царапины кнопок меню, заголовок без капель, «Летопись»→иконка-книга.

### 0.47 — Заход 4 «Орда гуще + экономика»
- +14 врагов до **21 типа** (гуль/скелет/утопленник/личинка/латник/гончая/бомба/лучник/культист/паук/огр/призрак/гарпия/жук) с поведениями (`behav`: +fast/exploder, паук split).
- **Конечные тематические волны:** `pickType` = трикл-фоддер, опасные типы — конечными группами в `doWave`. Экономика: монеты по силе врага.

### 0.44–0.46 — опыт/синергии/тряска
- Скорость кристаллов ×2; заплатка лавины опыта (`lvlQueue`≤3); синергия «Маяк»→узкий пронзающий луч-линия (spear, разворот 360°/с);
  пауза не трясёт камеру, макс тряска ×1.5 слабее/медленнее; **Стальная буря** = слияние Клинков+Меча в ОДИН большой меч (×2 урон, клинки скрыты); «Меч веры» ×0.5 урон и ×0.5 скорость.

### 0.43 — ПЕРЕСБОРКА механики опыта + Оберег + нерф Меча
- XP пересобран: лимит поля ~3 ур. (`xpFieldCap`), множитель времени запекается при дропе (`gemDrop`), постоянный магнит, кристаллы не исчезают, без пер-пикап каппов. Иконка пассивки «Оберег». Нерф «Меча веры».

### 0.42 — дев-панель с одного нажатия
- Дев-панель открывается ОДНИМ тапом по `#verCorner` (`toggleDev`).

### 0.41 — Заход 3 шаг 5: переработка синергий (5 слияний)
- Убраны 7 старых синергий, добавлен `FUSIONS` (5 пар, оба оружия lv5 → карта-слияние, фиолетовая связь HUD `.ichip.fused`, `isFused()`):
  storm/pyre/spear/knell + steel «Стальная буря». Ранее в заходе: бафы Креста/Святой воды, оружие **Меч веры** (+эво «Рой клинков»), защита от снарядов (пассив «Оберег»).

### 0.38–0.40 — айдентика + реворк оружия
- Меню на арте (`art/menubg.jpg`) + анимация (`drawMenuAmbient`: красные глаза, листья, луна, облака, туман). Aura → **«Кадило»** (дым/жар).
  Реворк Креста/Святой воды, новое оружие **«Меч»**, защита от снарядов. Звук непрерывного фонаря убран. Увеличена зона тапа по версии.

### 0.37 — Заход 1 «Стабилизация»
- **Честный фикс инфляции опыта:** босс роняет 0 прямого XP (награда = магнит), множитель времени применяется один раз. UI-полиш (убраны подсказки, сжаты карточки на мобиле, плоский HP-бар, `fmtNum` в сводке). **Шрифт Tiny5** (полная кириллица; VT323 её не содержал → откат на системный).

### 0.36 — HUD / мета / настройки
- HUD: полоса опыта вдвое тоньше (7px), полоса HP — тонкой рядом сверху (`#hpbartop`), цифры HP убраны.
- Мета (Склеп): апгрейд **«Переброс»** (старт 0, до 3). Базовые перебросы забега 3→0 (теперь только от меты/героя).
- Настройки: кнопка **«Сброс прогресса»** со счётчиком ×5 (5 кликов → defaultMeta). Кнопка фуллскрина минималистичная (без рамки).

### 0.35 — интерфейс + геймплейные правки
- Шрифт VT323 встроен **base64** (работает на `file://` — раньше Chrome блокировал `@font-face` по file://, шрифт откатывался на системный).
- Меню: мыши — медленный плавный взмах, мрачные облака, золото «приклеено» к «СКЛЕП», подпись музыки очень мелкой.
- Геймплей: сквош-анимация мобов втрое слабее (0.06→0.018), фонарь уже на старте (финал по ширине как был), маленький стартовый радиус сбора (`magBase` 90→38).

### 0.34 — пиксельное меню/настройки (доводка)
- Пиксельные шрифты во всём UI (h1/h2/.serif → VT323). Иконки на кнопках меню (человек/крест/свиток/череп/шестерёнка).
- Убрана строка «рекорд/уровень/убийств». Золото-чип у «СКЛЕП». Настройки: пиксель-тумблеры, «СИЛА ТРЯСКИ»→тумблер «ТРЯСКА КАМЕРЫ», крупные пиксельные «ЗВУК»/«ИГРА».

### 0.33 — анимация боссов + лужи + FPS
- **Босс-рендер по `BOSS_DRAW[bkind]`**: Матерь роя переписана как детальная анимированная паучиха (объёмное тело градиентами, 8 членистых перебирающих ног, светящиеся глаза, хелицеры, дыхание) вместо сквош-пульса. Каркас для качественной отрисовки остальных боссов.
- **Лужи** — кривые детальные пятна воды (неправильный контур + глубина + блики), не «интерактивные круги».
- FPS-счётчик перенесён в правый-нижний угол (не под полосой опыта).

### 0.32 — пиксельное меню (первый проход)
- Раскладка по эскизу владельца: крупная «▸ НА ОХОТУ» + золото-чип + ряд кнопок. Пиксельный шрифт **VT323** (кириллица), встроен в бандл (`fonts/VT323-Regular.ttf`, sw-кэш) для офлайна.
- Ночное небо переработано (`drawMenuAmbient`): без земли, без красного. Холодная луна с гало, слоёные мерцающие звёзды, дрейфующие облака, дымка, нетопыри с 2-кадровым взмахом.
- Подзаголовок → «НОЧЬ БУДЕТ ОЧЕНЬ ДОЛГОЙ». `#scrMenu` прозрачен (видно небо), `drawGround` не рисуется в меню.

### 0.31 — пилот: Матерь роя + Затонувшая деревня (Tier 3)
- Враг **рой-мошкара** (`gnat`, tiny swarm). `BOSS_MECH.matron` — кайт прочь, выводок мошкары (0 опыта), фазы, выброс на 33%. Уникальный спрайт `boss_matron` (рендер по `bkind`).
- Локация **Затонувшая деревня** (слот grave): водный тайл, декор (крыша/камыш/коряга/ряска), сваи-препятствия, водный хазард. Боссы по план C доходят до Утопленницы (финал).

### 0.30 — фундамент боссового арка (Tier 2)
- Рефактор поведения врагов в поле `behav` (chase/swarm/hop/ranged/tank/warp) + фикс белых версий (перебор `ENEMY_MAPS`).
- Система **препятствий**: редкие твёрдые коллайдеры детерминированно по миру (как декор), тормозят ТОЛЬКО игрока; снаряды/враги — сквозь. ~4 на экране (плотность — ручка). Спрайты пока общий камень; по локациям — Tier 3.
- **Каркас боссов + структура план C**: ростер `BOSSES` (8, фикс. порядок), `BOSS_MECH`-диспатч по боссу (пока generic), локация = индекс финала (`LOCS[].boss`), забег кончается на финале локации → `winGame`. Старый «Вечный Вампир на 15:00» убран.
- **Панель разработчика** `#devPanel`: секретный жест (5 тапов по `#verCorner`), обёртка над `__game` — старт локаций/героев, спавн боссов (+финал), оружие/пассивки максом, варп, **таймскейл** (`devTimescale`), читы, спавн врагов.
- ⚠️ Боссы пока с обобщённым ИИ; реальные механики (Выводок и т.д.) и переделка локаций под боссов — Tier 3+.

### 0.29 — хотфикс
- Баг **лавины опыта**: `gemDrop` не копит тысячи XP (≤40 кристаллов, потолок 100, без перелёта); `updateGems` стопит сбор при левел-апе в кадре → опыт зачисляется по одному уровню. Худший случай ~9 уровней по очереди (было 30–50 разом). Проверено в браузере.
- «Свеча» → «Магнит» (+25% радиус, новая иконка-подкова). Убран экранный счётчик серий (`#comboTag`); достижение «Кровавая жатва» и стата серий — фоном.

### 0.28 — интерфейс и бой
- Заголовок: кроваво-красный (#c01524) + анимированные капли крови (`.titlewrap`/`.drip`), мягкий glow.
- Луна в меню ярче/заметнее (drawMenuAmbient) — холодная серая, без крови, контраст сохранён.
- HUD крупнее: HP-бар 200→260, текст/строка крупнее. Иконки разделены: **оружие — 4 слота (включая
  пустые, лимит виден)** `#hudWeapons`, **пассивки/синергии** отдельно `#hudPassives`. refreshItems переписан.
- HP-бар и XP-бар мгновенные (убраны CSS transition).
- Снаряды перерисованы пиксельно с нуля: SPR.blade (клинок), SPR.scythe (крест), SPR.pbolt/_g (болт),
  вражеские eb — пиксельные орбы. drawBullets/drawBlades переписаны (+ фикс: орбита учитывает эволюцию).
- Кнопка «на весь экран» (#btnFull, угол). sw CACHE → 0.28.

### 0.12 — ребрендинг + UI/настройки
- Переименование в **LONG NIGHT**, версия показывается в меню.
- Ретема всех предметов под «сторону Света» (оружие/пассивки/синергии — новые имена и описания).
- **HP-бар рядом с героем** (рисуется над персонажем; тумблер в настройках).
- **Расширенная страница настроек:** громкость master/music/sfx с %, ползунок силы тряски,
  тумблеры: HP-бар у героя, цифры урона, режим производительности, счётчик FPS. Всё сохраняется
  в meta.settings и синхронизируется между вкладками.
- Прошлые фиксы (в предыдущих версиях до нумерации): потолок ценности кристаллов (лавина
  уровней после босса), реже сердца, мягче тряска, магнит на сердца, Retina/мобайл, тач-джойстик,
  глушение звука при громкости 0.

### 0.13 — пиксель-арт мира
- Все существа (герой + 6 врагов + босс) перерисованы в пиксель-арт через карты символов
  (`px(rows,pal,s)` + `addOutline`), с тёмной обводкой. `ctx.imageSmoothingEnabled=false` для
  чётких пикселей (вкл. обратно для виньетки). Факельный свет вокруг героя (радиальный градиент).
  Герой — 2 кадра ходьбы (`SPR.player`/`player_a`). «Освящённая земля» и «Святая вода»
  перекрашены в святое золото. **Производительность выросла:** 242 врага = 8.3 мс/кадр (было ~16).
### 0.14 — иконки предметов
- Пиксельная иконка каждому оружию/пассивке + общий «✦» для синергий (`SPR.icons`, `icoURL()`).
  Ряд иконок с уровнями в HUD (`#hudItems`, `refreshItems()`), иконки на карточках левел-апа.
### 0.15 — музыка (готик-оркестр, файловые треки)
- Папка `music/`: menu.mp3 (CC0, OpenGameArt) · calm.ogg (CC0) · intense.mp3 (CC-BY, 128k) ·
  boss.mp3 (CC-BY, 128k). Итого ~12.8 МБ. Старый процедурный синт-«бит» отключён (`A.musTick`
  пустой); SFX-синтез оставлен.
- `MUSIC` (объект): ленивая загрузка (меню сразу, остальные через 3с), кроссфейд по состоянию
  (меню / calm / intense по интенсивности / boss), мягкая деградация при ошибке трека,
  полная тишина при master=0 и пауза при скрытии вкладки. Атрибуция CC-BY — в настройках.
- **Чтобы менять музыку:** положить файлы в `music/` (и в деплой-репо), поправить `MUSIC.tracks`.

### 0.16 — сочный левел-ап + пикапы
- На экране левел-апа: **Переброс** (run.rerolls, старт 3), **Изгнать** (run.banishes, старт 1 —
  кликнуть карту в режиме изгнания, предмет в run.banished, исключается из rollUpgrades),
  **Пропустить** (+12 HP, +◆6). Хоткеи R / B. `cardClick()` маршрутизирует pick/banish.
- Пикапы на поле (kind в run.gems): **магнит** (притягивает все кристаллы/золото),
  **бомба** (урон всем в радиусе 380), **монета** (золото). Падают: монета ~1.2% с обычных,
  магнит/бомба с элит (50/50) и с боссов. Магнитятся опыт/золото/сердца; сундук/магнит/бомба — поднять самому.

### 0.17 — эволюции оружия
- `EVOS` (массив): оружие на макс + предмет-партнёр на нужном ур. → гарантированно предлагается
  золотая карта «★ ЭВОЛЮЦИЯ» (приоритет в `rollUpgrades`). `run.evo[id]=true`, `applyUpgrade`
  обрабатывает kind 'evo'. Пары: Арбалет+Серебряные пули(crit3)→Серебряный залп (веер 7 болтов);
  Клинки+Песочные часы(haste3)→Нимб; Молния+Наручи(might3)→Гнев небес (удар по площади в точках);
  Аура+Кираса(armor3)→Святилище (шире+лечит); Крест+Библия(hunger3)→Распятие; Вода+Елей(regen3)→Кара огнём.
  Эффекты — ветки `run.evo.X` в fireWeapons/fireChain/updateZones. Болты эволюции золотые (`b.evo`).

### 0.18 — выбор персонажа + анлоки
- `HEROES` (массив): Охотник(free,Арбалет) · Паладин(300,Освящённая земля,+25HP,+броня,−скор) ·
  Чернокнижник(450,Цепная молния,+18%урон,−20HP) · Инквизитор(600,Крест,+10%скор,+2 переброса).
  `meta.hero` (выбран), `meta.heroes` (открытые, hunter всегда). Экран `#scrHeroes` (buildHeroes),
  кнопка ГЕРОИ в меню, строка выбранного героя. newRun читает героя: стартовое оружие + моды статов.

### 0.19 — PWA (установка на телефон + офлайн)
- Файлы: `manifest.webmanifest`, `sw.js`, `icons/icon-{192,512,180}.png` (иконка — золотой крест +
  кровавая луна, сгенерирована через canvas в браузере, уменьшена `sips`). В `<head>` — manifest,
  theme-color, apple-touch-icon, apple-mobile-web-app-*. Регистрация SW в boot.
- SW: документ — network-first (свежая игра), ассеты (музыка/иконки) — cache-first → офлайн после
  первой загрузки. **ВАЖНО: при КАЖДОМ деплое менять `CACHE` в sw.js (сейчас 'longnight-0.19'),**
  иначе старые ассеты залипнут у игроков. Деплой теперь копирует и эти файлы.

### 0.20 — кульминация + полировка
- `WIN_TIME=900` (15:00): приходит **ВЕЧНЫЙ ВАМПИР** (`spawnFinalBoss`, `e.final`, ~42k HP),
  обычные боссы после этого не спавнятся. Убил его → `winGame()` → экран «НОЧЬ ПЕРЕЖИТА»
  (золотой заголовок, +800 бонус-золота). `buildDeath(won)` переключает заголовок/цвет.
- Фикс белой вспышки боссов: вместо полного белого спрайта — нормальный спрайт + мягкий
  бело-оверлей с alpha=flash*5 (не «блоб» при постоянном уроне).
- ВАЖНО про sw.js: CACHE поднят до 'longnight-0.20'.
- Не сделано (на будущее): покадровая анимация всех врагов (сейчас анимирован только герой +
  рантайм-сквош у всех), интро-слоумо босса.

### 0.21 — полировка UI
- Экраны: лёгкий `backdrop-filter:blur` (глубина, размывает фон-геймплей за паузой/левел-апом),
  прокрутка+падинги (ничего не режется на низких экранах), декор-разделитель `.divline` в меню.
- Кнопки: единый радиус, `:active`-состояние, ряды кнопок одинаковой ширины (`.btnrow .btn min-width`).
- Склеп: экспорт/импорт JSON свёрнут за кнопку «СОХРАНЕНИЕ / ЗАГРУЗКА ⏷» (меньше визуального шума).
- sw.js CACHE → 'longnight-0.21'.

### 0.22 — живой мир (ощущения)
- Покадровая анимация всех врагов: `ENEMY_MAPS` (таблица), `stepRows`/`shiftRow` генерят 2-й кадр
  ходьбы (нижние ряды в противофазе). `SPR[id]/_a/_w/_wa`. drawEnemy выбирает кадр по `e.t*7+e.id`.
- Хит-стоп: `hitStop`/`hitStopAdd` — слоумо (×0.12) на крупных ударах (смерть босса, урон игроку, эволюция).
- Комбо-счётчик: `run.combo/comboT/comboMax`, `#comboTag` (×N СЕРИЯ при ≥5, сброс через 2.5с).
  Макс. серия — в статистике смерти.
- Золотая вспышка `#goldflash`/`flashGold()` при левел-апе и эволюции. sw CACHE → 0.22.

### 0.23 — звук + статистика
- Музыка: +2 трека (music/final.mp3 = Lightless Dawn для финального босса, music/tension.mp3 =
  Ghost Story для боссов tier≥3). MUSIC.tick выбирает final/tension/boss/intense/calm/menu.
  Итого 6 треков ~20 МБ. `A.victory()` — триумфальный джингл в winGame. sw CACHE → 0.23, ASSETS += 2 трека.
- Постоянная статистика: `meta.stats` (runs/kills/bosses/wins/playtime/bestKills/locBest/heroBest/wpnDmg),
  `recordRun(won)` копит в gameOver/winGame. Экран **«ЛЕТОПИСЬ»** (`#scrStats`, buildStats) — кнопка в меню.

### 0.24 — бестиарий + достижения
- `meta.bestiary` (виденные типы, метятся в spawnEnemy), `ENEMY_INFO` (имя+описание). Экран
  **«БЕСТИАРИЙ»** (`#scrBest`, buildBestiary) — пиксель-портреты (sprURL из SPR[type]), неоткрытые под «?».
- `ACHIEVEMENTS` (8 шт), `meta.ach` (получено), `checkAchievements()` в recordRun → награда золотом +
  тост (showBanner). Список достижений — секция в экране «ЛЕТОПИСЬ». `stats.bestCombo` для combo50.
- Кнопка БЕСТИАРИЙ в меню. sw CACHE → 0.24.

### 0.25 — новые локации
- LOCS += **Затопленный склеп** (crypt, req 10:00, +20% золота: ледяные лужи замедляют ТОЛЬКО врагов,
  много бесов/нетопырей) и **Преисподняя** (inferno, req 14:00, +35%: частые огненные разломы,
  элита/громилы). Переиспользованы механики луж (swamp→swamp+crypt, синий цвет, игрок иммунен) и
  разломов (ash→ash+inferno, чаще). Свои тайлы/палитра в SPR.grounds; decors crypt=кости, inferno=угли.
  Веса спавна в pickType. sw CACHE → 0.25.

### 0.26 — новое оружие
- **Колокол** (bell): волна света, расходится кольцом от героя (run.rings, рост в updateZones,
  урон при пересечении). **Фонарь** (lantern): луч-конус в сторону ближайшего врага (тик урона
  по сектору, прицел run.lanternAim, отрисовка конуса в drawZones). Лимит 4 оружия — теперь выбор из 8.
- Эволюции: Колокол+Песочные часы→Благовест (чаще/шире), Фонарь+Наручи→Маяк (свет 360°). Иконки добавлены.
- sw CACHE → 0.26.

### 0.27 — чистка меню + UI/визуал
- Меню: убраны verTag/locRow/heroName/нижний хинт. Новый поток: **В БОЙ → выбор героя → выбор локации**
  (`launching` флаг; buildHeroes в режиме launching ведёт на `scrPickLoc`/buildPickLoc). Закрытые
  локации СКРЫТЫ (показаны только открытые по рекорду). Меню-кнопка ГЕРОИ — режим управления (launching=false).
- Заголовок: мягче цвет (#c7545f вместо кислотного #d4203a) + изящное свечение (titlepulse).
- Фон обесцвечен под контраст с UI: `.screen` нейтрально-серый, луна в drawMenuAmbient — бледно-серая (не кровавая).
- Иконки предметов перерисованы детальнее (тени/блики, IPAL +l/+n), рендер s=3→4 (выше разрешение).
  HUD-чипы 30→44px (моб. 24→34), иконки на карточках 42→46. sw CACHE → 0.27.

## Дальнейший план (roadmap)
- **Идеи на будущее (обсуждаемо):** доп. кадры анимации для всех врагов; пиксельные иконки
  получше; показ билда на паузе; эволюции оружия; выбор персонажа; пикапы (магнит/бомба/золото);
  reroll/skip/banish; экран победы на 30:00; бестиарий; больше треков (Ghost Story, Lightless Dawn).
- **(сделано 0.13)** — Пиксель-арт мира: спрайты, обводка, факельный свет, анимация, золотая аура.
- **(сделано 0.14)** — Иконки предметов + HUD ряд + иконки на карточках.
- **(сделано 0.15, архивная заметка)** — Музыка: прогрессивная загрузка готик-оркестр треков (папка `music/`),
  переключение по интенсивности (меню/бой/босс), кроссфейд. **ВАЖНО: FreePD.com мёртв (404).**
  Проверенные рабочие источники (агент скачивал и проверял ffprobe, июнь 2026):

  | Трек | URL | Лиц. | Длит. | Размер | Роль |
  |------|-----|------|-------|--------|------|
  | Oldschool Horror Theme (EmoPreben) | `https://opengameart.org/sites/default/files/Horror_1.mp3` | **CC0** | 3:18 | 4.5MB | меню/атмосфера |
  | Derelict (Ivan Stanton) | `https://opengameart.org/sites/default/files/derelict.ogg` | **CC0** | 2:43 | 1.8MB | спокойный бой |
  | Loaben (James Gargette) | `https://opengameart.org/sites/default/files/loaben_4.mp3` | **CC0** | — | 1.4MB | запас |
  | Ossuary 6 - Air (MacLeod) | `https://incompetech.com/music/royalty-free/mp3-royaltyfree/Ossuary%206%20-%20Air.mp3` | CC-BY | 4:10 | 7.6MB | напряжение |
  | Anxiety (MacLeod) | `…/mp3-royaltyfree/Anxiety.mp3` | CC-BY | 1:51 | 4.3MB | интенсивный бой |
  | Crypto (MacLeod) | `…/mp3-royaltyfree/Crypto.mp3` | CC-BY | 3:24 | 6.2MB | интенсивный бой |
  | Curse of the Scarab (MacLeod) | `…/mp3-royaltyfree/Curse%20of%20the%20Scarab.mp3` | CC-BY | 5:27 | 10MB | босс |
  | Lightless Dawn (MacLeod) | `…/mp3-royaltyfree/Lightless%20Dawn.mp3` | CC-BY | 6:20 | 14.5MB | босс/финал |

  - Incompetech mp3 идут в 256-325 kbps → перекодировать в 128k (`ffmpeg -i in.mp3 -b:a 128k out.mp3`),
    чтобы уложиться в ~20МБ. CC0-треки атрибуции не требуют; для CC-BY (Kevin MacLeod) добавить в
    меню/README строку: «"Track" Kevin MacLeod (incompetech.com), CC BY 4.0».
  - Первым грузить лёгкий CC0 (меню), остальные докачивать в фоне; при сбое продолжать что есть.
  - Полный отчёт агента с альтернативами — в истории сессии (Ghost Story, Despair and Triumph, Killers).
- **Дальше (обсуждаемо):** эволюции оружия, выбор персонажа, пикапы (магнит/бомба/золото),
  reroll/skip/banish на левел-апе, экран победы на 30:00, бестиарий.

## Открытые вопросы к владельцу
- Финальные названия предметов: агент предложил тематический набор (реализован в 0.12),
  владелец может переопределить любое имя.

---

## ✅ БАГ ОПЫТА — ВЫЛЕЧЕН ЧЕСТНО (0.59, 17.06.2026)
> РЕШЕНО: корень = двойной сбор мёртвых кристаллов (sweep пропускался при левел-апе). Фикс `if(g.dead) continue` в updateGems, заплатка lvlQueue≤3 убрана. Доказано ratio 1.000. Ниже — историческая запись.

> Несмотря на пересборку опыта (0.43: лимит поля ~3 ур., постоянный медленный магнит) и фикс инфляции (0.37),
> владелец ВСЁ ЕЩЁ иногда ловит «лавину» — выдаётся куча уровней разом (после магнита/босса или просто в какой-то момент).
> Корень окончательно НЕ найден.
- **СЕЙЧАС стоит ЗАПЛАТКА (0.45):** в `addXP` очередь левел-апа `lvlQueue` жёстко ограничена 3 — нельзя получить
  больше **3 уровней за раз** (излишек XP сбрасывается `run.xp=xpNext-1`). Это костыль, маскирует симптом, не лечит причину.
- **ЗАДАЧА (в самом конце):** найти ИСТИННЫЙ источник лавины (включить XP-лог `window.__xplog` через дев-панель,
  воспроизвести, посмотреть точный кадр/числа) и убрать заплатку. Подозреваемые: десинк `run.fieldXp`, двойной подбор
  кристалла (в `updateGems` нет `if(g.dead) continue` в начале — полагается на `sweep`), магнит + край `xpFieldCap`.

## 🎵 МУЗЫКА — ✅ ФАЙЛЫ ПОЛУЧЕНЫ (17.06.2026) — делается в ЗАХОДЕ 5
> **Владелец принёс 5 СОБСТВЕННЫХ треков** (написал сам) — лежат в `music/`: `1 трек.mp3` (1.9 МБ), `2 трек.mp3` (2.7 МБ),
> `3 трек.mp3` (1.8 МБ), `4 трек.mp3` (5.8 МБ), `5 трек.mp3` (5.9 МБ). Суммарно ~18 МБ — **тяжело, надо сжать.** ffmpeg есть.
> Эти файлы уже в рабочем дереве (git: старые menu/calm/intense/boss/tension/final удалены, новые «N трек» добавлены, untracked).
>
> **🎼 НАСТРОЕНИЕ И НАЗНАЧЕНИЕ ТРЕКОВ (запомнить — я не слышу аудио, ориентируюсь только на это описание владельца):**
> - **1 трек — драматичный, нагнетающий.** → ПЛЕЙ В МЕНЮ (экран меню).
> - **2 трек — лёгкий, волнующий, приключенческий.** → музыка УРОВНЕЙ (геймплей, «какие-то уровни»).
> - **3 трек — дружественный, спокойный, слегка грустный.** → музыка УРОВНЕЙ (геймплей, «какие-то уровни»).
> - **4 трек — энергичный, опасный.** → ВКЛЮЧАЕТСЯ ПРИ ПОЯВЛЕНИИ БОССА; после смерти игрока ИСЧЕЗАЕТ; переходы плавные.
> - **5 трек — нагнетающий, страшный, быстрый.** → (назначение точно не задано; вероятно поздняя стадия/опасный момент/финал — уточнить или выбрать самому по вайбу).
>
> **ЗАДАЧИ ЗАХОДА 5 ПО МУЗЫКЕ:**
> 1. **Сжать (минимальный вес):** ffmpeg в OGG/MP3 на низком битрейте (64–96k), при возможности моно, понизить частоту → вес в разы меньше. Результат в `music/`. Сохранить исходники владельца отдельно (не затирать, вдруг переделывать).
> 2. **Встроить в игру:** переименовать/переподключить в объекте `MUSIC` (game.html). Сейчас ключи menu/calm/intense/boss/tension/final — переразложить на новые 5 треков по назначению выше. Обновить `sw.js` ASSETS + поднять `CACHE`.
> 3. **Меню прослушивания (НОВОЕ):** экран/панель, где владелец может **прослушать все 5 треков по очереди и убедиться, что каждый прогрузился** (кнопка play на трек + индикатор загрузки/ошибки). Где разместить — настройки или отдельный пункт.
> 4. **Бесшовный плавный луп (НОВОЕ):** когда трек заканчивается — он же начинается снова, переход ПЛАВНЫЙ (кроссфейд конец→начало одного трека, не резкий обрыв). Реализация: два источника + перекрытие на N сек, либо crossfade-петля по `currentTime`.
> 5. **Убрать подпись названия трека в настройках** — сейчас в настройках показывается имя играющего трека (+атрибуция), владелец хочет это убрать (треки его собственные, атрибуция не нужна).
>
> **(Историческое) Бэкап-кандидаты и старая инструкция «владелец сам найдёт» — больше НЕ актуальны, файлы получены. Кандидаты ниже оставлены на случай, если владелец передумает.**

**Что делает ИИ, когда владелец принесёт файлы:**
- **Сжать (легче по весу):** перекодировать в OGG/MP3 на низком битрейте (64–128k), моно, понизить частоту,
  обрезать/зациклить → вес в разы меньше. Нужен `ffmpeg` (использовался в проекте). Кладёт результат в `music/`.
- **Опц. «битный»/лоу-фай вайб (по желанию владельца):** понизить sample rate (~8–22 кГц) + битность (8 бит) + bitcrusher
  → хрустящий ретро-звук под пиксель-арт. Степень регулируется. ⚠️ Это деградация оригинала, НЕ настоящий чиптюн
  (живую запись в 8-бит-мелодию сжатием не превратить — для чиптюна искать треки, уже написанные как chiptune).
- **Интеграция:** при желании — музыка по локациям (сейчас `MUSIC` в game.html переключается по интенсивности/боссу,
  НЕ по локации): добавить выбор трека по `run.loc`, поправить `MUSIC.tracks`, **файлы в `music/`** (+ в деплой-репо),
  поднять `sw CACHE`. CC0 = без атрибуции; CC-BY = подпись (мелкая уже есть в настройках).

**Бэкап-кандидаты** (если владелец передумает искать сам; ⭐ — рекомендация; были скачаны в `Музыка-выбор/`):
- **Затонувшая деревня:** ⭐ Loopable Dungeon Ambience (JaggedStone, CC0) · Dark Ambient Loop 13 (MundoSound, CC-BY) · Horror Atmosphere (SubspaceAudio, CC0)
- **Выжженные пастбища:** Desolate (Pixabay) · Echoes of Time v2 (K. MacLeod, CC-BY) · ⭐ Dark Ambience Loop (qubodup, CC-BY)
- **Погост:** ⭐ Ossuary 6 - Air (MacLeod, CC-BY) · Echoes of Time v2 (MacLeod) · Horror Atmosphere (CC0)
- **Заброшенная усадьба:** ⭐ Music Box Waltz (Pixabay) · Waltz Of The Forgotten (Pixabay) · Fairytale Waltz (MacLeod, CC-BY)
- **Тронный зал (финал):** ⭐ The Final Battle (skrjablin, CC0) · Epic Boss Battle (SubspaceAudio, CC0) · Sinister Boss Appears! (cynicmusic, CC0)
- Источники: opengameart.org (CC0/CC-BY), incompetech.com (MacLeod CC-BY), pixabay.com (Pixabay-лицензия). ⚠️ Pixabay блокирует ботов (403) — качать вручную.
